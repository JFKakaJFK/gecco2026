[project]
name = "voronoi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "numpy>=1.21.6",
    "pillow>=9.5.0",
    "pygom",
]

[tool.uv.sources]
pygom = { path = "../../../" }

# If you want to use mypy or pyright, you may have to ignore some errors, like below:

# mypy: disable-error-code="type-arg"

from __future__ import annotations

import datetime
import enum
import io
from typing import Any, Callable, List, Optional, Tuple, Union, overload

import numpy as np
import numpy.typing as npt

NumberType = (int, float, np.number)

# TODO the following stubs silence a few warnings, but aren't good documentations - fix that

class Rng:
    pass

# !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!  AUTOGENERATED CODE !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
# <litgen_stub> // Autogenerated code below! Do not edit!
####################    <generated_from:amalgamation.h>    ####################
# THIS FILE WAS GENERATED AUTOMATICALLY. DO NOT EDIT.

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin.h                                                                               //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_H
#

# clang-format off

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/lib/assert.h included by goblin.h                                               //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////

# TODO add a macro for error handling and input handling to that
# a simple runtime assert to check cheap invariants even with optimizations
# - use often, profile and only remove if runtime cost is considerable

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/lib/types.h included by goblin.h                                                //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_LIB_TYPES_H
#

# #define EIGEN_RUNTIME_NO_MALLOC  // enable runtime allocation testing

# currently the python conversion between nanobind/numpy is broken - discrete
# values + active don't translate?

# allow non-contiguous strides at the expense of vectorization...

# Is this a good idea?
# template <typename T>
# using Box = std::unique_ptr<T>;
# template <typename T>
# using Rc = std::shared_ptr<T>;

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/lib/rng.h included by goblin.h                                                  //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_LIB_RNG_H
#

# using Rng = std::mt19937;

# TODO possibly use the OpenRAND provided sampling methods (decreases rng portability, but it looks like any rng can be
# wrapped with openrand::BaseRNG<T> and the sampling methods both look convenient and decently fast)

# TODO possibly profile & look at (for faster rn generation)
# - https://www.pcg-random.org/posts/bounded-rands.html
# - https://github.com/swiftlang/swift/pull/39143#issue-comment-box

def permute(rng: Rng, n: int) -> List[int]:
    pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/lib/ordering.h included by goblin.h                                             //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_LIB_ORDERING_H
#

class Ordering(enum.IntEnum):
    Better = enum.auto()  # (= 0b10)
    Equal = enum.auto()  # (= 0b00)
    Worse = enum.auto()  # (= 0b01)
    NonDominated = enum.auto()  # (= 0b11)

# template <typename E>
#   requires std::is_enum_v<E> && requires { format_as(std::declval<E>()); }
# struct std::formatter<E>
#     : std::formatter<decltype(format_as(std::declval<E>()))> {
#   auto format(E e, auto &ctx) const {
#     return std::formatter<decltype(format_as(e))>::format(format_as(e), ctx);
#   }
# };

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/lib/fitness.h included by goblin.h                                              //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_LIB_FITNESS_H
#

class QualityBase:
    """/ Something that describes how good a solution is"""

    def clone(self) -> QualityBase:  # overridable (pure virtual)
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class FitnessBase:
    def num_objectives(self) -> int:  # overridable (pure virtual)
        pass

    def cmp(  # overridable (pure virtual)
        self, lhs: QualityBase, rhs: QualityBase, objective: Optional[int]
    ) -> Ordering:
        pass

    def distance(  # overridable (pure virtual)
        self, lhs: QualityBase, rhs: QualityBase, objective: Optional[int]
    ) -> float:
        pass

    def log_header(self, os: io.IOBase) -> None:  # overridable (pure virtual)
        pass

    def log(  # overridable (pure virtual)
        self, os: io.IOBase, quality: QualityBase
    ) -> None:
        pass

    def format(self, quality: QualityBase) -> str:  # overridable
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class ArchiveFitnessBase(FitnessBase):
    def worst(self) -> QualityBase:  # overridable (pure virtual)
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class MOQuality(QualityBase):
    """/ Something that describes how good a solution is"""

    def clone(self) -> QualityBase:
        pass
    objectives: np.ndarray
    constraint_value: float
    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class MOFitness(ArchiveFitnessBase):
    @overload
    def __init__(
        self, num_objectives: int, minimize: bool = True, epsilon: float = 0.0
    ) -> None:
        pass

    def log_header(self, os: io.IOBase) -> None:
        pass

    def log(self, os: io.IOBase, quality: QualityBase) -> None:
        pass

    def format(self, quality: QualityBase) -> str:
        pass

    def num_objectives(self) -> int:
        pass

    @overload
    def cmp(
        self, lhs: QualityBase, rhs: QualityBase, objective: Optional[int] = None
    ) -> Ordering:
        pass

    @overload
    def distance(
        self, lhs: QualityBase, rhs: QualityBase, objective: Optional[int] = None
    ) -> float:
        pass

    def worst(self) -> QualityBase:
        pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/lib/subset.h included by goblin.h                                               //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_LIB_SUBSET_H
#

class Subset:
    discrete: List[int]
    continuous: List[int]

    def clear(self) -> None:
        pass

    def size(self) -> int:
        pass

    def empty(self) -> bool:
        pass

    def similarity(self, other: Subset) -> float:
        pass

    def merge(self, other: Subset) -> Subset:
        pass

    def __init__(
        self,
        discrete: Optional[List[int]] = None,
        continuous: Optional[List[int]] = None,
    ) -> None:
        """Auto-generated default constructor with named params


        Python bindings defaults:
            If any of the params below is None, then its default value below will be used:
                * discrete: List[int]()
                * continuous: List[int]()
        """
        pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/lib/solution.h included by goblin.h                                             //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_LIB_SOLUTION_H
#

# Note the separate solution exists to hide the data ownership
# - without it, for operations using separate arenas like
# e.g. GOM with a donor from an archive the arena would have to be handled
# explicitly, but that is an implementation detail GOMEA shouldn't have to know
# about. And for other representations such as a vector of solution structs,
# this is also nicer.

class SolutionExtensionKey:
    token: Any = None
    def __init__(self) -> None:
        """Auto-generated default constructor"""
        pass

class SolutionExtensionKeyHash:
    def __call__(self, key: SolutionExtensionKey) -> int:
        pass

    def __init__(self) -> None:
        """Auto-generated default constructor"""
        pass

class SolutionExtensionBase:
    def clone(self) -> SolutionExtensionBase:  # overridable (pure virtual)
        pass

    def key(self) -> SolutionExtensionKey:  # overridable (pure virtual)
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class SolutionBase:
    def has_extension(  # overridable (pure virtual)
        self, key: SolutionExtensionKey
    ) -> bool:
        pass

    def get_or_insert_extension(  # overridable (pure virtual)
        self, extension: SolutionExtensionBase
    ) -> SolutionExtensionBase:
        """this is fine"""
        pass

    @overload
    def get_extension(  # overridable (pure virtual)
        self, key: SolutionExtensionKey
    ) -> Optional[SolutionExtensionBase]:
        pass

    @overload
    def get_extension(  # overridable (pure virtual)
        self, key: SolutionExtensionKey
    ) -> Optional[SolutionExtensionBase]:
        pass

    def remove_extension(  # overridable (pure virtual)
        self, key: SolutionExtensionKey
    ) -> bool:
        pass

    def clear_extensions(self) -> None:  # overridable (pure virtual)
        pass
    # Instead of vector, should this be an ExtensionProxy that behaves like a vector/iterator but does not allocate full
    # copies?? (size, begin, end, proxy to underlying collection)
    def num_extensions(self) -> int:  # overridable (pure virtual)
        pass

    @overload
    def extensions(self) -> List[SolutionExtensionBase]:  # overridable (pure virtual)
        pass

    @overload
    def extensions(self) -> List[SolutionExtensionBase]:  # overridable (pure virtual)
        pass

    def num_discrete(self) -> int:
        pass

    @overload
    def discrete_values(self) -> np.ndarray:  # overridable (pure virtual)
        pass

    @overload
    def discrete_values(self) -> np.ndarray:  # overridable (pure virtual)
        pass

    @overload
    def discrete_active(self) -> np.ndarray:  # overridable (pure virtual)
        pass

    @overload
    def discrete_active(self) -> np.ndarray:  # overridable (pure virtual)
        pass

    def num_continuous(self) -> int:
        pass

    @overload
    def continuous_values(self) -> np.ndarray:  # overridable (pure virtual)
        pass

    @overload
    def continuous_values(self) -> np.ndarray:  # overridable (pure virtual)
        pass

    @overload
    def continuous_active(self) -> np.ndarray:  # overridable (pure virtual)
        pass

    @overload
    def continuous_active(self) -> np.ndarray:  # overridable (pure virtual)
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

    def quality(self) -> QualityBase: ...

class Solution(SolutionBase):
    @overload
    def __init__(
        self,
        quality: QualityBase,
        discrete_values: Optional[np.ndarray] = None,
        continuous_values: Optional[np.ndarray] = None,
    ) -> None:
        pass

    @overload
    def __init__(self, other: Solution) -> None:
        pass

    @overload
    def __init__(self, other: Solution) -> None:
        pass

    @overload
    def __init__(self, s: SolutionBase) -> None:
        """explicitly not explicit since implicit conversion is the intent"""
        pass

    @overload
    def discrete_values(self) -> np.ndarray:
        pass

    @overload
    def discrete_values(self) -> np.ndarray:
        pass

    @overload
    def discrete_active(self) -> np.ndarray:
        pass

    @overload
    def discrete_active(self) -> np.ndarray:
        pass

    @overload
    def continuous_values(self) -> np.ndarray:
        pass

    @overload
    def continuous_values(self) -> np.ndarray:
        pass

    @overload
    def continuous_active(self) -> np.ndarray:
        pass

    @overload
    def continuous_active(self) -> np.ndarray:
        pass

    def has_extension(self, key: SolutionExtensionKey) -> bool:
        pass

    def get_or_insert_extension(
        self, extension: SolutionExtensionBase
    ) -> SolutionExtensionBase:
        pass

    @overload
    def get_extension(
        self, key: SolutionExtensionKey
    ) -> Optional[SolutionExtensionBase]:
        pass

    @overload
    def get_extension(
        self, key: SolutionExtensionKey
    ) -> Optional[SolutionExtensionBase]:
        pass

    def remove_extension(self, key: SolutionExtensionKey) -> bool:
        pass

    def clear_extensions(self) -> None:
        pass

    def num_extensions(self) -> int:
        pass

    @overload
    def extensions(self) -> List[SolutionExtensionBase]:
        pass

    @overload
    def extensions(self) -> List[SolutionExtensionBase]:
        pass

    def quality(self) -> QualityBase: ...

class SolutionSetBase:
    @overload
    def __getitem__(self, idx: int) -> SolutionBase:  # overridable (pure virtual)
        pass

    @overload
    def __getitem__(self, idx: int) -> SolutionBase:  # overridable (pure virtual)
        pass

    def size(self) -> int:  # overridable (pure virtual)
        pass

    def empty(self) -> bool:  # overridable (pure virtual)
        pass

    def add(self, s: SolutionBase) -> None:  # overridable (pure virtual)
        pass

    def remove_at(self, idx: int) -> None:  # overridable (pure virtual)
        pass

    def remove_indices_sorted(  # overridable (pure virtual)
        self, indices: List[int]
    ) -> None:
        pass

    def reserve(self, new_capacity: int) -> None:  # overridable (pure virtual)
        pass

    def clear(self) -> None:  # overridable (pure virtual)
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

# TODO possibly convert the runtime to normal asserts if they measurably impact performance

class AoSSet(SolutionSetBase):
    @overload
    def __getitem__(self, idx: int) -> SolutionBase:
        pass

    @overload
    def __getitem__(self, idx: int) -> SolutionBase:
        pass

    def size(self) -> int:
        pass

    def empty(self) -> bool:
        pass

    def add(self, s: SolutionBase) -> None:
        pass

    def remove_at(self, idx: int) -> None:
        pass

    def remove_indices_sorted(self, indices: List[int]) -> None:
        pass

    def reserve(self, new_capacity: int) -> None:
        pass

    def clear(self) -> None:
        pass

    def __init__(self) -> None:
        """Auto-generated default constructor"""
        pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/lib/archive.h included by goblin.h                                              //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_LIB_ARCHIVE_H
#

class ArchiveBase:
    def size(self) -> int:  # overridable (pure virtual)
        pass

    def empty(self) -> bool:
        pass

    def __getitem__(self, idx: int) -> SolutionBase:  # overridable (pure virtual)
        pass

    def unsafe_at(self, idx: int) -> SolutionBase:  # overridable (pure virtual)
        """Unsafe because by being in the archive we assume the invariant that the
        solution is non-dominated. Allowing potential changes to the solution
        without re-checking non-dominance hence is not "safe" from the logic
        perspective.
        """
        pass

    def change_count(self) -> int:
        pass

    def reset_change_count(self) -> None:
        pass

    def so_solution(self, objective: int) -> SolutionBase:  # overridable (pure virtual)
        pass

    def adapt(self) -> None:  # overridable
        pass

    def clear(self) -> None:  # overridable (pure virtual)
        pass

    def fitness(self) -> ArchiveFitnessBase:  # overridable (pure virtual)
        pass

    def clone(self) -> ArchiveBase:  # overridable (pure virtual)
        pass

    def update(
        self, solution: SolutionBase, strict: bool = True, check_synched: bool = True
    ) -> bool:
        """/ Updates the archive with the solution and returns whether the solution was
        / accepted into the archive.
        """
        pass

    def sync_with(self, other: ArchiveBase) -> None:
        pass

    def unsync_all(self) -> None:
        pass

    def dominates(self, solution: SolutionBase, strict: bool) -> bool:
        pass

    def covers(self, other: ArchiveBase) -> bool:
        pass

    def approximately_covers(self, other: ArchiveBase, epsilon: float) -> bool:
        """/ Checks if this archive approximately covers other, where
        / approximate coverage corresponds to the one-sided Hausdorff distance (or max[D_PF->S]) in objective space being
        / lower than some epsilon.
        """
        pass

    def random_solution(self, rng: Rng) -> SolutionBase:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class UnboundedArchive(ArchiveBase):
    def __init__(self, fitness: ArchiveFitnessBase) -> None:
        pass

    def clone(self) -> ArchiveBase:
        pass

    def size(self) -> int:
        pass

    def __getitem__(self, idx: int) -> SolutionBase:
        pass

    def unsafe_at(self, idx: int) -> SolutionBase:
        pass

    def so_solution(self, objective: int) -> SolutionBase:
        pass

    def clear(self) -> None:
        pass

    def update_archive(self, solution: SolutionBase, strict: bool) -> Tuple[bool, bool]:
        pass

    def fitness(self) -> ArchiveFitnessBase:
        pass

class AdaptiveGridArchive(ArchiveBase):
    def __init__(
        self,
        fitness: ArchiveFitnessBase,
        capacity: int,
        max_deviation: float = 0.25,
        max_iterations: int = 25,
        initial_discretization: Optional[float] = None,
    ) -> None:
        pass

    def clone(self) -> ArchiveBase:
        pass

    def size(self) -> int:
        pass

    def __getitem__(self, idx: int) -> SolutionBase:
        pass

    def unsafe_at(self, idx: int) -> SolutionBase:
        pass

    def clear(self) -> None:
        pass

    def so_solution(self, objective: int) -> SolutionBase:
        pass

    def update_archive(self, solution: SolutionBase, strict: bool) -> Tuple[bool, bool]:
        pass

    def adapt(self) -> None:
        pass

    def fitness(self) -> ArchiveFitnessBase:
        pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/lib/budget.h included by goblin.h                                               //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_LIB_BUDGET_H
#

class TerminationStatus(enum.IntEnum):
    TimeLimitReached = enum.auto()  # (= 0)
    GenerationLimitReached = enum.auto()  # (= 1)
    EvaluationLimitReached = enum.auto()  # (= 2)
    TargetReached = enum.auto()  # (= 3)
    Converged = enum.auto()  # (= 4)
    Aborted = enum.auto()  # (= 5)
    Running = enum.auto()  # (= 6)

class Budget:
    max_evaluations: Optional[int] = None
    max_generations: Optional[int] = None
    max_time: Optional[datetime.timedelta] = None
    termination_callback: Optional[Callable[[], bool]] = None

    def __init__(
        self,
        max_evaluations: Optional[int] = None,
        max_generations: Optional[int] = None,
        max_time_seconds: Optional[int] = None,
        max_duration: Optional[datetime.timedelta] = None,
        termination_callback: Optional[Callable[[], bool]] = None,
    ) -> None:
        pass

    def exhausted(
        self, generations: int, evaluations: int, elapsed: datetime.timedelta
    ) -> Optional[TerminationStatus]:
        pass

    def exhausted_or_external_criterion_met(
        self, generations: int, evaluations: int, elapsed: datetime.timedelta
    ) -> Optional[TerminationStatus]:
        pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/lib/instance.h included by goblin.h                                             //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_LIB_INSTANCE_H
#

# - (Single) solution values (derive objectives)
# - (Single) solution objectives (use dummy values, eval != actual fitness)
# TODO multiple solution values / objectives
#
#   => (a => a.covers(X))
# - Front size (a => a.size() >= X)
# - Front predicate (e.g. D(PF->S) <= X) => (a => a.distance_to(PF = a') < X)

class CacheKey:
    @overload
    def __init__(self, key: str) -> None:
        pass

    @overload
    def __init__(self, key: List[std.byte]) -> None:
        pass

    def __eq__(self, other: CacheKey) -> bool:
        pass

class InstanceBase:
    def num_objectives(self) -> int:
        pass

    def num_discrete(self) -> int:
        pass

    def discrete_domain_sizes(self) -> np.ndarray:  # overridable (pure virtual)
        pass

    def num_continuous(self) -> int:
        pass

    def continuous_lower_bounds(self) -> np.ndarray:  # overridable (pure virtual)
        pass

    def continuous_upper_bounds(self) -> np.ndarray:  # overridable (pure virtual)
        pass

    def continuous_init_lower_bounds(self) -> np.ndarray:  # overridable (pure virtual)
        pass

    def continuous_init_upper_bounds(self) -> np.ndarray:  # overridable (pure virtual)
        pass
    # TODO support ordinal discrete spaces (e.g. bool per discrete to indicate categorical/ordinal)
    # TODO support permutation spaces (e.g. bool per continuous variable to indicate continuous/random keys)

    def evaluate(  # overridable (pure virtual)
        self, rng: Rng, solutions: SolutionSetBase, indices: List[int]
    ) -> None:
        pass

    def evaluate_partial(  # overridable
        self,
        rng: Rng,
        solutions: SolutionSetBase,
        parents: SolutionSetBase,
        subsets: List[Subset],
        indices: List[int],
    ) -> None:
        pass

    def evaluate_solutions(
        self, solutions: SolutionSetBase, seed: Optional[int] = None
    ) -> None:
        pass

    def adapt(self, rng: Rng) -> bool:  # overridable
        """/ Possibly adapts the problem in some way that may require re-evaluating any elites stored thus far (indicated by
        / the return value)
        """
        pass

    def gradients(  # overridable
        self,
        rng: Rng,
        solutions: SolutionSetBase,
        parents: SolutionSetBase,
        subsets: List[Subset],
        indices: List[int],
        evaluations: int,
    ) -> np.ndarray:
        """/ Returns the gradient for each index of indices (row) and continuous variable (column) with respect to the
        / optimization goal. The number of evaluations performed to calculate the gradients are added to `evaluations`;
        /
        / Note: The default implementation assumes that the passes subsets contain all active indices and the finite
        / difference approach used requires up to `2 * indices.size() * num_continuous()` evaluations.
        """
        pass

    def gradient_steps(  # overridable
        self,
        rng: Rng,
        solutions: SolutionSetBase,
        parents: SolutionSetBase,
        indices: List[int],
        num_steps: int,
    ) -> Tuple[List[int], int]:
        """/ Performs gradient optimization steps of the continuous variables and returns the
        / subset of `indices` that changed together with the number of evaluations performed.
        / Guarantees that the quality of all solutions is up-to-date.
        """
        pass

    def add_random(  # overridable (pure virtual)
        self, rng: Rng, solutions: SolutionSetBase, count: int
    ) -> None:
        pass

    def fitness(self) -> FitnessBase:  # overridable (pure virtual)
        pass

    def archive_fitness(self) -> ArchiveFitnessBase:  # overridable (pure virtual)
        """/ The fitness the archive should be use, in case the archive should record the front in more objectives than what
        / should be optimized (as per https://arxiv.org/abs/2507.03777v1)
        /
        / Note: What `fitness()` optimizes must always be a compatible subset of what `archive_fitness()` optimizes.
        """
        pass
    # // corresponds to e.g. ERCs / one constant per edge in GP
    # virtual bool always_inherit_continuous() const { return False; };

    def inherit_discrete(  # overridable
        self, offspring: SolutionBase, donor: SolutionBase, subset: Subset
    ) -> Tuple[bool, bool]:
        """/ The offspring inherits a subset of the decision variables from the donor, returning True if there was a change to
        / the active variables and an evaluation is needed, possibly with problem specific modifications.
        /
        / Returns a `(any_active_changed, anything_changed)` tuple
        """
        pass

    def as_continuous(  # overridable
        self, solution: SolutionBase, discrete_index: int
    ) -> Optional[float]:
        """useful for discrete linkage learning in GP
        - needed to be able to perform constant binning
        - needed to erase constant pool references
        """
        pass

    def target_reached(self, archive: ArchiveBase) -> bool:
        pass
    # InstanceBase& add_target(AnyTarget target);
    @overload
    def add_target(
        self, target_reached_check: Callable[[ArchiveBase], bool]
    ) -> InstanceBase:
        pass

    @overload
    def add_target(self, target_front: ArchiveBase) -> InstanceBase:
        pass

    @overload
    def add_target(self, target_solution: SolutionBase) -> InstanceBase:
        pass

    @overload
    def add_target(self, target_quality: QualityBase) -> InstanceBase:
        pass

    @overload
    def add_target(
        self, target_objectives: Union[np.ndarray, List[float]]
    ) -> InstanceBase:
        pass

    def add_target_front_size(self, target_front_size: int) -> InstanceBase:
        pass

    def add_target_front(
        self,
        discrete: Optional[np.ndarray],
        continuous: Optional[np.ndarray],
        evaluation_seed: Optional[int] = None,
    ) -> InstanceBase:
        pass

    @overload
    def add_target(self, target_fitness: float) -> InstanceBase:
        pass
    #
    #  InstanceBase& add_any_target(AnyTarget target) {
    #    // return std::visit([&](auto&& arg) -> InstanceBase& { return add_target(arg); }, target);
    #    //
    #    // return
    #    std::visit([&](auto&& arg) { add_target(arg); }, target);
    #    return *this;
    #
    #    // return std::visit(
    #    //     [&](auto&& arg) -> InstanceBase& {
    #    //       using T = std::decay_t<decltype(arg)>;
    #    //       if constexpr (std::is_same_v<T, CType>) {
    #    //         return add_target(arg);
    #    //       } else if constexpr (std::is_same_v<T, Vec<CType>>) {
    #    //         return add_target(arg);
    #    //       } else if constexpr (std::is_same_v<T, std::vector<CType>>) {
    #    //         return add_target(arg);
    #    //       } else if constexpr (std::is_same_v<T, std::reference_wrapper<const QualityBase>>) {
    #    //         return add_target(arg);
    #    //       } else if constexpr (std::is_same_v<T, std::reference_wrapper<const SolutionBase>>) {
    #    //         return add_target(arg);
    #    //       } else if constexpr (std::is_same_v<T, std::reference_wrapper<const ArchiveBase>>) {
    #    //         return add_target(arg);
    #    //       } else if constexpr (std::is_same_v<T, std::function<bool(const ArchiveBase&)>>) {
    #    //         return add_target(arg);
    #    //       } else {
    #    //         static_assert(sizeof(T) == 0, "non-exhaustive visitor!");
    #    //       }
    #    //     },
    #    //     target);
    #
    #    // std::visit(overloaded{
    #    //                [&](CType arg) { add_target(arg); },
    #    //                [&](Vec<CType> arg) { add_target(arg); },
    #    //                [&](std::vector<CType> arg) { add_target(arg); },
    #    //                [&](std::reference_wrapper<const QualityBase> arg) { add_target(arg); },
    #    //                [&](std::reference_wrapper<const SolutionBase> arg) { add_target(arg); },
    #    //                [&](std::reference_wrapper<const ArchiveBase> arg) { add_target(arg); },
    #    //                [&](std::function<bool(const ArchiveBase&)> arg) { add_target(arg); },
    #    //            },
    #    //            target);
    #
    #    // if (std::holds_alternative<CType>(target)) {
    #    //   return add_target(std::get<CType>(target));
    #    //   // } else if (std::holds_alternative<Vec<CType>>(target)) {
    #    //   // return add_target(std::get<Vec<CType>>(target));
    #    // } else if (std::holds_alternative<std::vector<CType>>(target)) {
    #    //   return add_target(std::get<std::vector<CType>>(target));
    #    // } else {
    #    //   throw std::runtime_error("Non exhaustive visitor.");
    #    // }
    #  };
    #

    # virtual bool target_reached(const ArchiveBase& archive) const { return False; };

    def log_header(self, os: io.IOBase) -> None:  # overridable
        pass

    def log_solution(
        self, os: io.IOBase, solution: SolutionBase
    ) -> None:  # overridable
        pass

    def log(self, os: io.IOBase, solution: SolutionBase) -> None:  # overridable
        pass

    def format_solution(self, solution: SolutionBase) -> str:
        pass

    def solution_cache_key(  # overridable
        self, solution: SolutionBase
    ) -> Optional[CacheKey]:
        """/ By returning std::nullopt, caching can be disabled on a per solution basis"""
        pass

    def reevaluate_and_rebuild_archive(self, rng: Rng, archive: ArchiveBase) -> int:
        pass

    def unwrap(self) -> InstanceBase:  # overridable
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class WrappedInstance(InstanceBase):
    """/ Intermediate class for wrapping instances that by default forwards everything to the actual inner method. Still
    / allows accidentally not overwriting some methods, but at least defaults to the behaviour of the wrapped instance
    / instead of the default implementations of virtual methods.
    """

    def __init__(self, instance: InstanceBase) -> None:
        pass

    def discrete_domain_sizes(self) -> np.ndarray:
        pass

    def continuous_lower_bounds(self) -> np.ndarray:
        pass

    def continuous_upper_bounds(self) -> np.ndarray:
        pass

    def continuous_init_lower_bounds(self) -> np.ndarray:
        pass

    def continuous_init_upper_bounds(self) -> np.ndarray:
        pass

    def evaluate(
        self, rng: Rng, solutions: SolutionSetBase, indices: List[int]
    ) -> None:
        pass

    def evaluate_partial(
        self,
        rng: Rng,
        solutions: SolutionSetBase,
        parents: SolutionSetBase,
        subsets: List[Subset],
        indices: List[int],
    ) -> None:
        pass

    def adapt(self, rng: Rng) -> bool:
        pass

    def gradients(
        self,
        rng: Rng,
        solutions: SolutionSetBase,
        parents: SolutionSetBase,
        subsets: List[Subset],
        indices: List[int],
        evaluations: int,
    ) -> np.ndarray:
        pass

    def gradient_steps(
        self,
        rng: Rng,
        solutions: SolutionSetBase,
        parents: SolutionSetBase,
        indices: List[int],
        num_steps: int,
    ) -> Tuple[List[int], int]:
        pass

    def add_random(self, rng: Rng, solutions: SolutionSetBase, count: int) -> None:
        pass

    def fitness(self) -> FitnessBase:
        pass

    def archive_fitness(self) -> ArchiveFitnessBase:
        pass

    def inherit_discrete(
        self, offspring: SolutionBase, donor: SolutionBase, subset: Subset
    ) -> Tuple[bool, bool]:
        pass

    def as_continuous(
        self, solution: SolutionBase, discrete_index: int
    ) -> Optional[float]:
        pass

    def target_reached(self, archive: ArchiveBase) -> bool:
        pass

    def log_header(self, os: io.IOBase) -> None:
        pass

    def log_solution(self, os: io.IOBase, solution: SolutionBase) -> None:
        pass

    def log(self, os: io.IOBase, solution: SolutionBase) -> None:
        pass

    def solution_cache_key(self, solution: SolutionBase) -> Optional[CacheKey]:
        pass

    def unwrap(self) -> InstanceBase:
        pass

class CachedInstanceBase(WrappedInstance):
    def __init__(self, instance: InstanceBase) -> None:
        pass

    def hit_count(self) -> int:  # overridable (pure virtual)
        pass

    def miss_count(self) -> int:  # overridable (pure virtual)
        pass

    def access_count(self) -> int:  # overridable (pure virtual)
        pass

    def entry_invalidation_count(self) -> int:  # overridable (pure virtual)
        pass

    def cache_invalidation_count(self) -> int:  # overridable (pure virtual)
        pass

    def evicted_count(self) -> int:  # overridable (pure virtual)
        pass

    def hit_ratio(self) -> float:  # overridable (pure virtual)
        pass

    def miss_ratio(self) -> float:  # overridable (pure virtual)
        pass

    def utilization(self) -> float:  # overridable (pure virtual)
        """/ Proportion of cache entries used w.r.t. maximum size"""
        pass

def Cached(
    problem: InstanceBase, cache_size: int = 10000, cache_policy: str = "lru"
) -> CachedInstanceBase:
    pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/lib/algorithms/upgma.h included by goblin.h                                     //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_LIB_UPGMA_H
#

class UPGMA:
    """/ Arthur's UPGMA implementation from
    / https://github.com/8uurg/Impact-of-Asynchrony-on-MBEAs/blob/07083732629661c7efccddb4c1b15e69cc462e/EALib/src/gomea.cpp#L216
    / This implementation was chosen because it returns the whole dendrogram, it
    / is commented better and Arthur's code was easier to adapt to work with Eigen
    / matrices directly...
    """

    class Merge:
        left: int
        right: int
        distance: float
        size: int
        def __init__(
            self,
            left: Optional[int] = None,
            right: Optional[int] = None,
            distance: Optional[float] = None,
            size: Optional[int] = None,
        ) -> None:
            """Auto-generated default constructor with named params


            Python bindings defaults:
                If any of the params below is None, then its default value below will be used:
                    * left: int()
                    * right: int()
                    * distance: float()
                    * size: int()
            """
            pass

    @staticmethod
    def cluster(rng: Rng, similarity: np.ndarray) -> List[UPGMA.Merge]:
        """/ UPGMA that clusters based on similarity (higher = closer), not distance
        / (lower = closer)
        """
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/lib/algorithms/subset_selection.h included by goblin.h                          //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_LIB_SUBSET_SELECTION_H
#

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/lib/algorithms/mo.h included by goblin.h                                        //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_LIB_ALGORITHMS_MO_H
#

@overload
def hypervolume2D(
    solutions: ArchiveBase, fitness: FitnessBase, reference_point: QualityBase
) -> float:
    pass

@overload
def hypervolume2D(
    solutions: SolutionSetBase, fitness: FitnessBase, reference_point: QualityBase
) -> float:
    pass

@overload
def hypervolume2D(points: np.ndarray, reference_point: np.ndarray) -> float:
    pass

# namespace goblin

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/lib/linkage_model.h included by goblin.h                                        //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_LIB_LINKAGE_MODEL_H
#

class VariableSet(enum.IntEnum):
    Discrete = enum.auto()  # (= 0b01)
    Continuous = enum.auto()  # (= 0b10)
    Mixed = enum.auto()  # (= 0b11)

def estimate_entropy(
    problem: InstanceBase,
    solutions: SolutionSetBase,
    indices: List[int],
    subset: List[int],
    intron_strategy: str,
    merge_continuous: bool,
    num_continuous_bins: Optional[int],
) -> np.ndarray:
    pass

class LinkageModelBase:
    # LinkageModelBase() = default;
    # LinkageModelBase(const LinkageModelBase&) = delete;
    # LinkageModelBase(LinkageModelBase&&) = delete;

    # LinkageModelBase& operator=(const LinkageModelBase&) = delete;
    # LinkageModelBase& operator=(LinkageModelBase&&) = delete;

    def init(  # overridable (pure virtual)
        self,
        rng: Rng,
        problem: InstanceBase,
        solutions: SolutionSetBase,
        variables: VariableSet,
    ) -> None:
        pass

    def subsets(  # overridable (pure virtual)
        self,
        rng: Rng,
        problem: InstanceBase,
        solutions: SolutionSetBase,
        indices: List[int],
        covariance: Optional[np.ndarray],
    ) -> FOS:
        pass

    def is_static(self) -> bool:  # overridable
        pass

    def clone(self) -> LinkageModelBase:  # overridable (pure virtual)
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class UnivariateFOS(LinkageModelBase):
    """
    (final class)
    """

    def __init__(self, subset: Optional[Subset] = None) -> None:
        pass

    def clone(self) -> LinkageModelBase:
        pass

    def init(
        self,
        rng: Rng,
        problem: InstanceBase,
        solutions: SolutionSetBase,
        variables: VariableSet,
    ) -> None:
        pass

    def subsets(
        self,
        rng: Rng,
        problem: InstanceBase,
        solutions: SolutionSetBase,
        indices: List[int],
        covariance: Optional[np.ndarray] = None,
    ) -> FOS:
        pass

    def is_static(self) -> bool:
        pass

class StaticFOS(LinkageModelBase):
    """
    (final class)
    """

    def __init__(self, fos: FOS) -> None:
        pass

    def clone(self) -> LinkageModelBase:
        pass

    def init(
        self,
        rng: Rng,
        problem: InstanceBase,
        solutions: SolutionSetBase,
        variables: VariableSet,
    ) -> None:
        pass

    def subsets(
        self,
        rng: Rng,
        problem: InstanceBase,
        solutions: SolutionSetBase,
        indices: List[int],
        covariance: Optional[np.ndarray] = None,
    ) -> FOS:
        pass

    def is_static(self) -> bool:
        pass

    def get(self) -> FOS:
        pass

class FullFOS(LinkageModelBase):
    """
    (final class)
    """

    def __init__(self, subset: Optional[Subset] = None) -> None:
        pass

    def clone(self) -> LinkageModelBase:
        pass

    def init(
        self,
        rng: Rng,
        problem: InstanceBase,
        solutions: SolutionSetBase,
        variables: VariableSet,
    ) -> None:
        pass

    def subsets(
        self,
        rng: Rng,
        problem: InstanceBase,
        solutions: SolutionSetBase,
        indices: List[int],
        covariance: Optional[np.ndarray] = None,
    ) -> FOS:
        pass

    def is_static(self) -> bool:
        pass

class LinkageTreeFOS(LinkageModelBase):
    """
    (final class)
    """

    def __init__(
        self,
        metric: str = "mi",
        intron_strategy: str = "none",
        merge_continuous: bool = True,
        num_continuous_bins: Optional[int] = None,
        filter_parent_threshold: Optional[float] = None,
        filter_children_threshold: Optional[float] = None,
        filter_root: Optional[bool] = None,
        max_subset_size: Optional[int] = None,
        normalize_initial_linkage_bias: bool = False,
        subset: Optional[Subset] = None,
        custom_similarity: Optional[np.ndarray] = None,
        eta_custom_similarity: Optional[float] = None,
        custom_similarity_agg: Optional[str] = None,
        similarity_callback: Optional[Callable[[np.ndarray], None]] = None,
        freeze: bool = False,
    ) -> None:
        pass

    def clone(self) -> LinkageModelBase:
        pass

    def is_static(self) -> bool:
        pass

    def register_similarity_callback(
        self, similarity_callback: Callable[[np.ndarray], None]
    ) -> None:
        pass

    def unregister_similarity_callback(self) -> None:
        pass

    def init(
        self,
        rng: Rng,
        problem: InstanceBase,
        solutions: SolutionSetBase,
        variables: VariableSet,
    ) -> None:
        pass

    def compute_similarity(
        self,
        rng: Rng,
        problem: InstanceBase,
        solutions: SolutionSetBase,
        indices: List[int],
        covariance: Optional[np.ndarray] = None,
    ) -> np.ndarray:
        pass

    def learn_lt(
        self, rng: Rng, similarity: np.ndarray, filter_root_default: bool
    ) -> FOS:
        pass

    def learn_lt_original(
        self, rng: Rng, similarity: np.ndarray, filter_root_default: bool
    ) -> FOS:
        pass

    def subsets(
        self,
        rng: Rng,
        problem: InstanceBase,
        solutions: SolutionSetBase,
        indices: List[int],
        covariance: Optional[np.ndarray] = None,
    ) -> FOS:
        pass

class CombinedFOS(LinkageModelBase):
    """
    (final class)
    """

    @overload
    def __init__(self, linkage_models: List[LinkageModelBase]) -> None:
        pass

    def add_model(self, model: LinkageModelBase) -> None:
        pass

    @overload
    def __init__(self, other: CombinedFOS) -> None:
        """Copy operations must copy the underlying models manually..."""
        pass

    @overload
    def __init__(self, param_0: CombinedFOS) -> None:
        """But moving is allowed"""
        pass

    def clone(self) -> LinkageModelBase:
        pass

    def init(
        self,
        rng: Rng,
        problem: InstanceBase,
        solutions: SolutionSetBase,
        variables: VariableSet,
    ) -> None:
        pass

    def subsets(
        self,
        rng: Rng,
        problem: InstanceBase,
        solutions: SolutionSetBase,
        indices: List[int],
        covariance: Optional[np.ndarray] = None,
    ) -> FOS:
        pass

    def is_static(self) -> bool:
        pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/lib/method.h included by goblin.h                                               //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_LIB_METHOD_H
#

class MethodBase:
    def run(  # overridable (pure virtual)
        self,
        problem: InstanceBase,
        budget: Budget,
        seed: Optional[int],
        population_size: Optional[int],
    ) -> Tuple[ArchiveBase, TerminationStatus]:
        pass
    # TODO scipy.optimize compatible .minimize function

    def current_generation(self) -> Optional[int]:  # overridable
        """/ Evaluations used and time elapsed can be collected without knowing about
        / method details, but not the generation/iteraction. This method provides a
        / hook for tracking the progress over generations
        """
        pass

    def current_population(self) -> Optional[Tuple[int, int, int]]:  # overridable
        """/ Size, generations and restarts of the currently active population if available for multi-start schemes"""
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/lib/init.h included by goblin.h                                                 //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_LIB_INIT_H
#

class InitBase:
    def sample(  # overridable (pure virtual)
        self, rng: Rng, problem: InstanceBase, count: int
    ) -> Tuple[np.ndarray, np.ndarray]:
        pass

    def add_random(
        self, rng: Rng, problem: InstanceBase, solutions: SolutionSetBase, count: int
    ) -> None:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class DiscreteInitBase:
    def sample(  # overridable (pure virtual)
        self, rng: Rng, problem: InstanceBase, count: int
    ) -> np.ndarray:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class ContinuousInitBase:
    def sample(  # overridable (pure virtual)
        self, rng: Rng, problem: InstanceBase, count: int
    ) -> np.ndarray:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class RandomDInit(DiscreteInitBase):
    """
    (final class)
    """

    def sample(self, rng: Rng, problem: InstanceBase, count: int) -> np.ndarray:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class RandomCInit(ContinuousInitBase):
    """
    (final class)
    """

    def sample(self, rng: Rng, problem: InstanceBase, count: int) -> np.ndarray:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class RandomInit(InitBase):
    """
    (final class)
    """

    def sample(
        self, rng: Rng, problem: InstanceBase, count: int
    ) -> Tuple[np.ndarray, np.ndarray]:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class CombinedInit(InitBase):
    """
    (final class)
    """

    def __init__(
        self,
        discrete: Optional[DiscreteInitBase] = None,
        continuous: Optional[ContinuousInitBase] = None,
    ) -> None:
        pass

    def sample(
        self, rng: Rng, problem: InstanceBase, count: int
    ) -> Tuple[np.ndarray, np.ndarray]:
        pass

def from_any_init(any_init: AnyInit) -> InitBase:
    pass

class CompleteInit(DiscreteInitBase):
    """
    (final class)
    """

    def sample(self, rng: Rng, problem: InstanceBase, count: int) -> np.ndarray:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/gp/instance.h included by goblin.h                                              //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_GP_INSTANCE_H
#

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/gp/context.h included by goblin/gp/instance.h                                   //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_GP_CONTEXT_H
#

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/gp/operator.h included by goblin/gp/context.h                                   //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_GP_OPERATOR_H
#

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/gp/template.h included by goblin/gp/operator.h                                  //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_GP_TEMPLATE_H
#

class TemplateNode:
    children: List[TemplateNode]

    @staticmethod
    def full_nary(branching_factor: int, depth: int) -> TemplateNode:
        pass

    def size(self) -> int:
        pass

    def max_num_children(self) -> int:
        pass

    def is_tree(self) -> bool:
        pass

    def is_cycle_free(self) -> bool:
        pass

    def __init__(self, children: Optional[List[TemplateNode]] = None) -> None:
        """Auto-generated default constructor with named params


        Python bindings defaults:
            If children is None, then its default value will be: List[TemplateNode]()
        """
        pass

class Template:
    outputs: List[TemplateNode]
    subexpressions: List[TemplateNode]

    @overload
    def __init__(self) -> None:
        pass

    @overload
    def __init__(
        self, outputs: List[TemplateNode], subexpressions: List[TemplateNode]
    ) -> None:
        pass

    def size(self) -> int:
        pass

    def max_num_children(self) -> int:
        pass

    def add_output(self, output: TemplateNode) -> None:
        pass

    def add_subtree(self, subexpression: TemplateNode) -> None:
        pass

    def is_valid(self) -> bool:
        pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/gp/operator.h continued                                                         //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////

# TODO multi-arity (not here), but allow nodes to not use all children but a
# chosen subset (=partial permutation)
# TODO types - every column (input and output Arr2Drices) now has a type etc...
# enum struct Type: unsigned char {
#     Bool = 0,
#     Float = 1
# };
# struct Signature {
#     std::vector<Type> args;
#     Type rty;
# };
# using CColRef = std::variant<
#     CRefS<Array<BType>>,
#     CRefS<Array<CType>>
# >;
# using ColRef = std::variant<
#     RefS<Array<BType>>,
#     RefS<Array<CType>>
# >;
# template<typename C>
# inline constexpr Type typeof(const C& col){
#     if (std::same_as<typename C::Scalar, CType>){
#         return Type::Float;
#     } else if constexpr (std::same_as<typename C::Scalar, BType>){
#         return Type::Bool;
#     } else {
#         std::unreachable();
#     }
# };

# The operators and their derivatives were more or less copied over from
# https://github.com/matigekunstintelligentie/MultiGPG/blob/main/src/operator.hpp

class OperatorBase:
    def min_arity(self) -> int:  # overridable (pure virtual)
        pass

    def max_arity(self) -> int:  # overridable (pure virtual)
        pass

    def is_commutative(self) -> bool:  # overridable (pure virtual)
        pass

    def apply(  # overridable (pure virtual)
        self, out: np.ndarray, args: np.ndarray
    ) -> None:
        pass

    def apply_buf(  # overridable
        self, buf: np.ndarray, out: int, args: List[int]
    ) -> None:
        pass

    def has_gradient(self) -> bool:  # overridable
        pass

    def apply_grad(  # overridable
        self, out: np.ndarray, d_out: np.ndarray, args: np.ndarray, d_args: np.ndarray
    ) -> None:
        pass

    def __call__(self, args: np.ndarray) -> np.ndarray:
        pass

    def format(self, args: List[str]) -> str:  # overridable (pure virtual)
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class OpIdentity(OperatorBase):
    def min_arity(self) -> int:
        pass

    def max_arity(self) -> int:
        pass

    def is_commutative(self) -> bool:
        pass

    def apply(self, out: np.ndarray, args: np.ndarray) -> None:
        pass

    def apply_buf(self, buf: np.ndarray, out: int, args: List[int]) -> None:
        pass

    def has_gradient(self) -> bool:
        pass

    def apply_grad(
        self, out: np.ndarray, d_out: np.ndarray, args: np.ndarray, d_args: np.ndarray
    ) -> None:
        pass

    def format(self, args: List[str]) -> str:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class OpAdd(OperatorBase):
    def min_arity(self) -> int:
        pass

    def max_arity(self) -> int:
        pass

    def is_commutative(self) -> bool:
        pass

    def apply(self, out: np.ndarray, args: np.ndarray) -> None:
        pass

    def apply_buf(self, buf: np.ndarray, out: int, args: List[int]) -> None:
        pass

    def has_gradient(self) -> bool:
        pass

    def apply_grad(
        self, out: np.ndarray, d_out: np.ndarray, args: np.ndarray, d_args: np.ndarray
    ) -> None:
        pass

    def format(self, args: List[str]) -> str:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class OpSub(OperatorBase):
    def min_arity(self) -> int:
        pass

    def max_arity(self) -> int:
        pass

    def is_commutative(self) -> bool:
        pass

    def apply(self, out: np.ndarray, args: np.ndarray) -> None:
        pass

    def apply_buf(self, buf: np.ndarray, out: int, args: List[int]) -> None:
        pass

    def has_gradient(self) -> bool:
        pass

    def apply_grad(
        self, out: np.ndarray, d_out: np.ndarray, args: np.ndarray, d_args: np.ndarray
    ) -> None:
        pass

    def format(self, args: List[str]) -> str:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class OpMul(OperatorBase):
    def min_arity(self) -> int:
        pass

    def max_arity(self) -> int:
        pass

    def is_commutative(self) -> bool:
        pass

    def apply(self, out: np.ndarray, args: np.ndarray) -> None:
        pass

    def apply_buf(self, buf: np.ndarray, out: int, args: List[int]) -> None:
        pass

    def has_gradient(self) -> bool:
        pass

    def apply_grad(
        self, out: np.ndarray, d_out: np.ndarray, args: np.ndarray, d_args: np.ndarray
    ) -> None:
        pass

    def format(self, args: List[str]) -> str:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class OpDiv(OperatorBase):
    def min_arity(self) -> int:
        pass

    def max_arity(self) -> int:
        pass

    def is_commutative(self) -> bool:
        pass

    def apply(self, out: np.ndarray, args: np.ndarray) -> None:
        pass

    def apply_buf(self, buf: np.ndarray, out: int, args: List[int]) -> None:
        pass

    def has_gradient(self) -> bool:
        pass

    def apply_grad(
        self, out: np.ndarray, d_out: np.ndarray, args: np.ndarray, d_args: np.ndarray
    ) -> None:
        pass

    def format(self, args: List[str]) -> str:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class OpSin(OperatorBase):
    def min_arity(self) -> int:
        pass

    def max_arity(self) -> int:
        pass

    def is_commutative(self) -> bool:
        pass

    def apply(self, out: np.ndarray, args: np.ndarray) -> None:
        pass

    def apply_buf(self, buf: np.ndarray, out: int, args: List[int]) -> None:
        pass

    def has_gradient(self) -> bool:
        pass

    def apply_grad(
        self, out: np.ndarray, d_out: np.ndarray, args: np.ndarray, d_args: np.ndarray
    ) -> None:
        pass

    def format(self, args: List[str]) -> str:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class OpCos(OperatorBase):
    def min_arity(self) -> int:
        pass

    def max_arity(self) -> int:
        pass

    def is_commutative(self) -> bool:
        pass

    def apply(self, out: np.ndarray, args: np.ndarray) -> None:
        pass

    def apply_buf(self, buf: np.ndarray, out: int, args: List[int]) -> None:
        pass

    def has_gradient(self) -> bool:
        pass

    def apply_grad(
        self, out: np.ndarray, d_out: np.ndarray, args: np.ndarray, d_args: np.ndarray
    ) -> None:
        pass

    def format(self, args: List[str]) -> str:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class OpExp(OperatorBase):
    def min_arity(self) -> int:
        pass

    def max_arity(self) -> int:
        pass

    def is_commutative(self) -> bool:
        pass

    def apply(self, out: np.ndarray, args: np.ndarray) -> None:
        pass

    def apply_buf(self, buf: np.ndarray, out: int, args: List[int]) -> None:
        pass

    def has_gradient(self) -> bool:
        pass

    def apply_grad(
        self, out: np.ndarray, d_out: np.ndarray, args: np.ndarray, d_args: np.ndarray
    ) -> None:
        pass

    def format(self, args: List[str]) -> str:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class OpLog(OperatorBase):
    def min_arity(self) -> int:
        pass

    def max_arity(self) -> int:
        pass

    def is_commutative(self) -> bool:
        pass

    def apply(self, out: np.ndarray, args: np.ndarray) -> None:
        pass

    def apply_buf(self, buf: np.ndarray, out: int, args: List[int]) -> None:
        pass

    def has_gradient(self) -> bool:
        pass

    def apply_grad(
        self, out: np.ndarray, d_out: np.ndarray, args: np.ndarray, d_args: np.ndarray
    ) -> None:
        pass

    def format(self, args: List[str]) -> str:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class OpSquare(OperatorBase):
    def min_arity(self) -> int:
        pass

    def max_arity(self) -> int:
        pass

    def is_commutative(self) -> bool:
        pass

    def apply(self, out: np.ndarray, args: np.ndarray) -> None:
        pass

    def apply_buf(self, buf: np.ndarray, out: int, args: List[int]) -> None:
        pass

    def has_gradient(self) -> bool:
        pass

    def apply_grad(
        self, out: np.ndarray, d_out: np.ndarray, args: np.ndarray, d_args: np.ndarray
    ) -> None:
        pass

    def format(self, args: List[str]) -> str:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class OpSqrt(OperatorBase):
    def min_arity(self) -> int:
        pass

    def max_arity(self) -> int:
        pass

    def is_commutative(self) -> bool:
        pass

    def apply(self, out: np.ndarray, args: np.ndarray) -> None:
        pass

    def apply_buf(self, buf: np.ndarray, out: int, args: List[int]) -> None:
        pass

    def has_gradient(self) -> bool:
        pass

    def apply_grad(
        self, out: np.ndarray, d_out: np.ndarray, args: np.ndarray, d_args: np.ndarray
    ) -> None:
        pass

    def format(self, args: List[str]) -> str:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class OpPow(OperatorBase):
    def min_arity(self) -> int:
        pass

    def max_arity(self) -> int:
        pass

    def is_commutative(self) -> bool:
        pass

    def apply(self, out: np.ndarray, args: np.ndarray) -> None:
        pass

    def apply_buf(self, buf: np.ndarray, out: int, args: List[int]) -> None:
        pass

    def has_gradient(self) -> bool:
        pass

    def apply_grad(
        self, out: np.ndarray, d_out: np.ndarray, args: np.ndarray, d_args: np.ndarray
    ) -> None:
        pass

    def format(self, args: List[str]) -> str:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class OpAbs(OperatorBase):
    def min_arity(self) -> int:
        pass

    def max_arity(self) -> int:
        pass

    def is_commutative(self) -> bool:
        pass

    def apply(self, out: np.ndarray, args: np.ndarray) -> None:
        pass

    def apply_buf(self, buf: np.ndarray, out: int, args: List[int]) -> None:
        pass

    def has_gradient(self) -> bool:
        pass

    def apply_grad(
        self, out: np.ndarray, d_out: np.ndarray, args: np.ndarray, d_args: np.ndarray
    ) -> None:
        pass

    def format(self, args: List[str]) -> str:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class OpMin(OperatorBase):
    def min_arity(self) -> int:
        pass

    def max_arity(self) -> int:
        pass

    def is_commutative(self) -> bool:
        pass

    def apply(self, out: np.ndarray, args: np.ndarray) -> None:
        pass

    def apply_buf(self, buf: np.ndarray, out: int, args: List[int]) -> None:
        pass

    def has_gradient(self) -> bool:
        pass

    def apply_grad(
        self, out: np.ndarray, d_out: np.ndarray, args: np.ndarray, d_args: np.ndarray
    ) -> None:
        pass

    def format(self, args: List[str]) -> str:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class OpMax(OperatorBase):
    def min_arity(self) -> int:
        pass

    def max_arity(self) -> int:
        pass

    def is_commutative(self) -> bool:
        pass

    def apply(self, out: np.ndarray, args: np.ndarray) -> None:
        pass

    def apply_buf(self, buf: np.ndarray, out: int, args: List[int]) -> None:
        pass

    def has_gradient(self) -> bool:
        pass

    def apply_grad(
        self, out: np.ndarray, d_out: np.ndarray, args: np.ndarray, d_args: np.ndarray
    ) -> None:
        pass

    def format(self, args: List[str]) -> str:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/gp/context.h continued                                                          //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ConstantRepr(enum.IntEnum):
    ERCs = enum.auto()  # (= 0)
    Edges = enum.auto()  # (= 1)
    Pool = enum.auto()  # (= 2)
    None_ = enum.auto()  # (= 3)

class ValueKind(enum.IntEnum):
    Input = enum.auto()  # (= 0)  # input feature idx
    Constant = enum.auto()  # (= 1)  # constant marker/pool idx
    Operator = enum.auto()  # (= 2)  # operator idx
    Arg = enum.auto()  # (= 3)  # subfunction argument idx
    Subtree = enum.auto()  # (= 4)  # subtree idx
    Parameter = enum.auto()  # (= 5)  # function class parameter idx

class GPContext:
    """/ The lookup tables needed to map the linear representation to the encoded
    / semantics and the methods for computing the output, active nodes and sympy
    / conversion.
    /
    / A two-step approach is used, where each semantic symbol (e.g. operators or
    / input features) are mapped to a value. However, the domain for e.g. leaf
    / nodes does not contain all values, so a second mapping is used to map
    / between node domain and value.
    """

    def __init__(
        self,
        num_inputs: int,
        expression_template: Template,
        operators: List[OperatorBase],
        num_parameters: int = 0,
        constant_representation: str = "ercs",
        constant_pool_size: int = 10,
        enable_subfunctions: bool = False,
        max_expression_size: Optional[int] = None,
        use_apply_buf: bool = True,
    ) -> None:
        pass

    def value2domain(self, index: int, value: int) -> Optional[int]:
        pass

    def parent(self, index: int) -> Optional[int]:
        pass

    def debug_log_expressions(
        self,
        os: io.IOBase,
        solution: SolutionBase,
        node: Optional[int] = None,
        indent: str = "",
    ) -> None:
        """A helper that prints the expression in a human readable format"""
        pass

    def to_sympy(self, solution: SolutionBase) -> List[str]:
        pass

    def normalized_root_proximity(self) -> np.ndarray:
        """Matrix of size `num_discrete x num_discrete`, where the entry i,j
        corresponds to the average proximity to the subtree root of nodes i and j (1.0 is close, 0.0 is distant)
        if both are from the same tree, otherwise 0
        """
        pass

    def normalized_node_proximity(self) -> np.ndarray:
        """Normalized node proximity [1.0: same node, 0.0: no connection]"""
        pass

    def normalized_wVIG(self) -> np.ndarray:
        """Normalized node proximity [1.0: same node, 0.0: no connection]"""
        pass

    def subtree_co_occurrences(self) -> np.ndarray:
        pass

    def copy_tree(
        self,
        source: SolutionBase,
        source_node: int,
        target: SolutionBase,
        target_node: int,
    ) -> bool:
        pass

    def active_nodes(self, solution: SolutionBase) -> List[int]:
        pass

    def active_constant_indices(self, solution: SolutionBase) -> List[int]:
        pass

    def inherit_shifted(
        self, solution: SolutionBase, donor: SolutionBase, node: int, num_levels: int
    ) -> Optional[Tuple[bool, int]]:
        pass
    # // TODO allow gradients w.r.t. specific continuous indices OR parameter
    # / indices
    # template <typename Scalar>
    # Arr2D<Scalar> compute_outputs_grad(SolutionBase& solution, Arr2D<Scalar>& X, Array<Scalar>& params) const {
    #   std::unreachable();
    # };

    # // std::string to_dot(const SolutionBase &solution) const {
    # /   std::unreachable();
    # / };

    const_repr: ConstantRepr

    num_inputs: int
    num_outputs: int
    num_subexpressions: int
    num_discrete: int
    num_continuous: int
    max_expression_size: int
    num_parameters: int
    max_num_children: int
    enable_subfunctions: bool
    use_apply_buf: bool

    operators: List[OperatorBase]
    op_idx2value: List[int]

    value_kind: List[ValueKind]
    value_min_arity: List[int]
    value_max_arity: List[int]
    value_idx: List[int]

    subtree_roots: List[int]  # indices of all subtree root nodes
    output_roots: List[int]  # indices of all output root nodes

    domain_sizes: np.ndarray  # node -> domain size
    domain2value: np.ndarray  # node domain -> value

    root: List[int]  # node -> current tree root
    sizes: List[int]  # node -> size of subtree starting at node,
    # ignoring inactive values
    depth: List[int]  # node -> node depth
    height: List[int]  # node -> node height
    children: List[List[int]]  # node -> child node indices

    nodes: List[
        List[int]
    ]  # node -> indices corresponding to the subtree starting at this
    # node (without subtrees)

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/gp/instance.h continued                                                         //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////

class GPInstanceBase(InstanceBase):
    def context(self) -> GPContext:  # overridable (pure virtual)
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class PyGPInstance(GPInstanceBase):
    def __init__(
        self,
        ctx: GPContext,
        evaluate: Callable[
            [Callable[[np.ndarray], Optional[np.ndarray]], List[str], int],
            Tuple[np.ndarray, float],
        ],
        num_objectives: int,
        init: AnyInit,
        minimize: bool = True,
        erc_init_lb: float = -10.0,
        erc_init_ub: float = 10.0,
    ) -> None:
        pass

    def discrete_domain_sizes(self) -> np.ndarray:
        pass

    def continuous_lower_bounds(self) -> np.ndarray:
        pass

    def continuous_upper_bounds(self) -> np.ndarray:
        pass

    def continuous_init_lower_bounds(self) -> np.ndarray:
        pass

    def continuous_init_upper_bounds(self) -> np.ndarray:
        pass

    def evaluate(
        self, rng: Rng, solutions: SolutionSetBase, indices: List[int]
    ) -> None:
        pass

    def add_random(self, rng: Rng, solutions: SolutionSetBase, count: int) -> None:
        pass

    def fitness(self) -> FitnessBase:
        pass

    def archive_fitness(self) -> ArchiveFitnessBase:
        pass

    def inherit_discrete(
        self, offspring: SolutionBase, donor: SolutionBase, subset: Subset
    ) -> Tuple[bool, bool]:
        pass

    def as_continuous(
        self, solution: SolutionBase, discrete_index: int
    ) -> Optional[float]:
        pass

    def log_solution(self, os: io.IOBase, solution: SolutionBase) -> None:
        pass

    def context(self) -> GPContext:
        pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/gp/init.h included by goblin.h                                                  //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_GP_INIT_H
#

class GrowInit(DiscreteInitBase):
    """
    (final class)
    """

    def __init__(
        self, p_terminal: Optional[float] = None, p_constant: Optional[float] = None
    ) -> None:
        pass

    def sample(self, rng: Rng, problem: InstanceBase, count: int) -> np.ndarray:
        pass

class FullInit(DiscreteInitBase):
    """
    (final class)
    """

    def __init__(self, p_constant: Optional[float] = None) -> None:
        pass

    def sample(self, rng: Rng, problem: InstanceBase, count: int) -> np.ndarray:
        pass

class HalfHalfInit(DiscreteInitBase):
    """
    (final class)
    """

    def __init__(
        self, p_terminal: Optional[float] = None, p_constant: Optional[float] = None
    ) -> None:
        pass

    def sample(self, rng: Rng, problem: InstanceBase, count: int) -> np.ndarray:
        pass

class PTC2Init(DiscreteInitBase):
    """/ PTC2 as per https://cs.gmu.edu/~sean/papers/treecreation.pdf
    (final class)
    """

    def __init__(self, p_constant: Optional[float] = None) -> None:
        pass

    def sample(self, rng: Rng, problem: InstanceBase, count: int) -> np.ndarray:
        pass

class RecursiveCompleteInit(DiscreteInitBase):
    """/ Complete init does not really work as intended for GP, since a lot of the diversity is distributed to inactive
    / individuals. This version aims to have the active and inactive values per variable be "probabilistically complete"
    / in isolation.
    (final class)
    """

    def sample(self, rng: Rng, problem: InstanceBase, count: int) -> np.ndarray:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class RecursiveCompleteInit2(DiscreteInitBase):
    """/ Complete init does not really work as intended for GP, since a lot of the diversity is distributed to inactive
    / individuals. This version aims to have the active and inactive values per variable be "probabilistically complete"
    / in isolation.
    (final class)
    """

    def __init__(
        self, p_terminal: Optional[float] = None, p_constant: Optional[float] = None
    ) -> None:
        pass

    def sample(self, rng: Rng, problem: InstanceBase, count: int) -> np.ndarray:
        pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/gp/sr.h included by goblin.h                                                    //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_GP_SR_H
#

class SRQuality(MOQuality):
    def clone(self) -> QualityBase:
        pass
    # / Linear scaling parameters
    ls_params: np.ndarray

    # /*
    #  The test accuracy uses interior mutability (i.e. it ignores const) since it is not
    #  part of what defines a solution or its accuracy - as indicated by the name, it is never
    #  used to make any decisions and only tracked for analysis purposes. By making it mutable
    #  it can be lazily computed only when requested.
    #   */
    # / Optional test set accuracy
    test_quality: Optional[MOQuality] = None
    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class SRFitness(MOFitness):
    def __init__(
        self, num_objectives: int, minimize: bool = True, epsilon: float = 0.0
    ) -> None:
        pass

    def worst(self) -> QualityBase:
        pass

class SRProblem(GPInstanceBase):
    def __init__(
        self,
        ctx: GPContext,
        X_train: np.ndarray,
        Y_train: np.ndarray,
        X_test: Optional[np.ndarray] = None,
        Y_test: Optional[np.ndarray] = None,
        objectives: Union[str, List[str]] = "mse",
        objectives_to_optimize: Optional[int] = None,
        linear_scaling: bool = True,
        init: Optional[AnyInit] = None,
        constant_init_lower_bound: float = -1.0,
        constant_init_upper_bound: float = 1.0,
        target_objectives: Optional[List[float]] = None,
        gradient_mode: str = "forward",
        gradient_epsilon: float = 1e-5,
        archive_epsilon: float = 0.0,
        always_inherit_continuous: Optional[bool] = None,
        batch_size: Optional[int] = None,
    ) -> None:
        pass

    def adapt(self, rng: Rng) -> bool:
        pass

    def discrete_domain_sizes(self) -> np.ndarray:
        pass

    def continuous_lower_bounds(self) -> np.ndarray:
        pass

    def continuous_upper_bounds(self) -> np.ndarray:
        pass

    def continuous_init_lower_bounds(self) -> np.ndarray:
        pass

    def continuous_init_upper_bounds(self) -> np.ndarray:
        pass

    def evaluate(
        self, rng: Rng, solutions: SolutionSetBase, indices: List[int]
    ) -> None:
        pass

    def add_random(self, rng: Rng, solutions: SolutionSetBase, count: int) -> None:
        pass

    def fitness(self) -> FitnessBase:
        pass

    def archive_fitness(self) -> ArchiveFitnessBase:
        pass

    def inherit_discrete(
        self, offspring: SolutionBase, donor: SolutionBase, subset: Subset
    ) -> Tuple[bool, bool]:
        pass

    def as_continuous(
        self, solution: SolutionBase, discrete_index: int
    ) -> Optional[float]:
        pass

    def log_header(self, os: io.IOBase) -> None:
        pass

    def evaluate_test(self, solution: SolutionBase) -> None:
        pass

    def log(self, os: io.IOBase, solution: SolutionBase) -> None:
        pass

    def log_solution(self, os: io.IOBase, solution: SolutionBase) -> None:
        pass

    def gradient_steps(
        self,
        rng: Rng,
        solutions: SolutionSetBase,
        parents: SolutionSetBase,
        indices: List[int],
        num_steps: int,
    ) -> Tuple[List[int], int]:
        pass

    def context(self) -> GPContext:
        pass
    ctx: GPContext
    linear_scaling: bool
    objectives: List[str]
    X_train: np.ndarray
    Y_train: np.ndarray
    var_Y_train: np.ndarray
    X_batch: np.ndarray
    Y_batch: np.ndarray
    var_Y_batch: np.ndarray
    X_test: np.ndarray
    Y_test: np.ndarray
    var_Y_test: np.ndarray

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/bench/functions.h included by goblin.h                                          //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_BENCH_FUNCTIONS_H
#

class ObjectiveBase:
    def num_discrete(self) -> int:  # overridable (pure virtual)
        pass

    def num_continuous(self) -> int:  # overridable (pure virtual)
        pass

    def evaluate(  # overridable (pure virtual)
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
    ) -> Tuple[float, float]:
        pass

    def evaluate_partial(  # overridable
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
        parent_discrete_values: np.ndarray,
        parent_continuous_values: np.ndarray,
        parent_discrete_active: np.ndarray,
        parent_continuous_active: np.ndarray,
        parent_objective_value: float,
        parent_constraint_value: float,
        discrete_indices: List[int],
        continuous_indices: List[int],
    ) -> Tuple[float, float]:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class MOFunctionBase:
    def num_objectives(self) -> int:  # overridable (pure virtual)
        pass

    def num_discrete(self) -> int:  # overridable (pure virtual)
        pass

    def num_continuous(self) -> int:  # overridable (pure virtual)
        pass

    def evaluate(self, solution: SolutionBase) -> None:  # overridable (pure virtual)
        pass

    def evaluate_partial(  # overridable
        self, solution: SolutionBase, parent: SolutionBase, subset: Subset
    ) -> None:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/bench/functions/combinators.h included by goblin.h                              //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_BENCH_FUNCTIONS_COMBINATORS_H
#

# TODO
# - [ ] Permute (scramble arguments to function, either fixed or random perm)
# - [ ] Translate (translate the continuous function inputs, fixed
# transformation or random)
# - [x] Rotate (rotate continuous function inputs, fixed angle or random per
# variable pair)
# - [x] Sum (add multiple functions)
# - [x] Concat (add smaller functions to make a larger function)
#   - [ ] Allow overlap
# - [x] Repeat (repeat the same function multiple times)
#   - [ ] Allow overlap
# - [x] Min (return min of several functions)
# - [x] Max (return max of several functions)
# - [x] BBO (no partial evals)
# - [x] Masked (no active information)
# - [?] Conditional - for doing something with introns?
#       For this to be useful, ideally Objectivebase
#       overloads a lot of operators to allow constructing
#       bools from `ObjectiveBase`s (e.g. Rastrigin(3) < Sphere(3))
# - [ ] Testing

class BBO(ObjectiveBase):
    """
    (final class)
    """

    def __init__(self, objective: ObjectiveBase) -> None:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
    ) -> Tuple[float, float]:
        pass

class Masked(ObjectiveBase):
    """
    (final class)
    """

    def __init__(self, objective: ObjectiveBase) -> None:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
    ) -> Tuple[float, float]:
        pass

    def evaluate_partial(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
        parent_discrete_values: np.ndarray,
        parent_continuous_values: np.ndarray,
        parent_discrete_active: np.ndarray,
        parent_continuous_active: np.ndarray,
        parent_objective_value: float,
        parent_constraint_value: float,
        discrete_indices: List[int],
        continuous_indices: List[int],
    ) -> Tuple[float, float]:
        pass

class Inverted(ObjectiveBase):
    """
    (final class)
    """

    def __init__(self, objective: ObjectiveBase) -> None:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
    ) -> Tuple[float, float]:
        pass

    def evaluate_partial(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
        parent_discrete_values: np.ndarray,
        parent_continuous_values: np.ndarray,
        parent_discrete_active: np.ndarray,
        parent_continuous_active: np.ndarray,
        parent_objective_value: float,
        parent_constraint_value: float,
        discrete_indices: List[int],
        continuous_indices: List[int],
    ) -> Tuple[float, float]:
        pass

class Translated(ObjectiveBase):
    """
    (final class)
    """

    def __init__(
        self, objective: ObjectiveBase, translation_vector: np.ndarray
    ) -> None:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
    ) -> Tuple[float, float]:
        pass

    def evaluate_partial(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
        parent_discrete_values: np.ndarray,
        parent_continuous_values: np.ndarray,
        parent_discrete_active: np.ndarray,
        parent_continuous_active: np.ndarray,
        parent_objective_value: float,
        parent_constraint_value: float,
        discrete_indices: List[int],
        continuous_indices: List[int],
    ) -> Tuple[float, float]:
        pass

class Rotated(ObjectiveBase):
    """
    (final class)
    """

    @overload
    def __init__(
        self,
        objective: ObjectiveBase,
        rotation_angle: float = -45.0,
        rotation_block_size: Optional[int] = None,
    ) -> None:
        pass

    @overload
    def __init__(
        self,
        objective: ObjectiveBase,
        seed: int,
        rotation_block_size: Optional[int] = None,
    ) -> None:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
    ) -> Tuple[float, float]:
        pass

    def evaluate_partial(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
        parent_discrete_values: np.ndarray,
        parent_continuous_values: np.ndarray,
        parent_discrete_active: np.ndarray,
        parent_continuous_active: np.ndarray,
        parent_objective_value: float,
        parent_constraint_value: float,
        discrete_indices: List[int],
        continuous_indices: List[int],
    ) -> Tuple[float, float]:
        pass

class Sum(ObjectiveBase):
    """
    (final class)
    """

    def __init__(self, objectives: List[ObjectiveBase]) -> None:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
    ) -> Tuple[float, float]:
        pass

    def evaluate_partial(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
        parent_discrete_values: np.ndarray,
        parent_continuous_values: np.ndarray,
        parent_discrete_active: np.ndarray,
        parent_continuous_active: np.ndarray,
        parent_objective_value: float,
        parent_constraint_value: float,
        discrete_indices: List[int],
        continuous_indices: List[int],
    ) -> Tuple[float, float]:
        pass

class Max(ObjectiveBase):
    """
    (final class)
    """

    def __init__(self, objectives: List[ObjectiveBase]) -> None:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
    ) -> Tuple[float, float]:
        pass

    def evaluate_partial(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
        parent_discrete_values: np.ndarray,
        parent_continuous_values: np.ndarray,
        parent_discrete_active: np.ndarray,
        parent_continuous_active: np.ndarray,
        parent_objective_value: float,
        parent_constraint_value: float,
        discrete_indices: List[int],
        continuous_indices: List[int],
    ) -> Tuple[float, float]:
        pass

class Min(ObjectiveBase):
    """
    (final class)
    """

    def __init__(self, objectives: List[ObjectiveBase]) -> None:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
    ) -> Tuple[float, float]:
        pass

    def evaluate_partial(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
        parent_discrete_values: np.ndarray,
        parent_continuous_values: np.ndarray,
        parent_discrete_active: np.ndarray,
        parent_continuous_active: np.ndarray,
        parent_objective_value: float,
        parent_constraint_value: float,
        discrete_indices: List[int],
        continuous_indices: List[int],
    ) -> Tuple[float, float]:
        pass

class Concat(ObjectiveBase):
    """
    (final class)
    """

    def __init__(self, objectives: List[ObjectiveBase]) -> None:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
    ) -> Tuple[float, float]:
        pass

    def evaluate_partial(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
        parent_discrete_values: np.ndarray,
        parent_continuous_values: np.ndarray,
        parent_discrete_active: np.ndarray,
        parent_continuous_active: np.ndarray,
        parent_objective_value: float,
        parent_constraint_value: float,
        discrete_indices: List[int],
        continuous_indices: List[int],
    ) -> Tuple[float, float]:
        pass

class Repeat(ObjectiveBase):
    """
    (final class)
    """

    def __init__(self, objective: ObjectiveBase, repeats: int) -> None:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
    ) -> Tuple[float, float]:
        pass

    def evaluate_partial(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
        parent_discrete_values: np.ndarray,
        parent_continuous_values: np.ndarray,
        parent_discrete_active: np.ndarray,
        parent_continuous_active: np.ndarray,
        parent_objective_value: float,
        parent_constraint_value: float,
        discrete_indices: List[int],
        continuous_indices: List[int],
    ) -> Tuple[float, float]:
        pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/bench/functions/discrete.h included by goblin.h                                 //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_BENCH_FUNCTIONS_DISCRETE_H
#

class OneMax(ObjectiveBase):
    """
    (final class)
    """

    def __init__(self, ndims: int) -> None:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
    ) -> Tuple[float, float]:
        pass

    def evaluate_partial(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
        parent_discrete_values: np.ndarray,
        parent_continuous_values: np.ndarray,
        parent_discrete_active: np.ndarray,
        parent_continuous_active: np.ndarray,
        parent_objective_value: float,
        parent_constraint_value: float,
        discrete_indices: List[int],
        continuous_indices: List[int],
    ) -> Tuple[float, float]:
        pass

class ZeroMax(ObjectiveBase):
    """
    (final class)
    """

    def __init__(self, ndims: int) -> None:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
    ) -> Tuple[float, float]:
        pass

    def evaluate_partial(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
        parent_discrete_values: np.ndarray,
        parent_continuous_values: np.ndarray,
        parent_discrete_active: np.ndarray,
        parent_continuous_active: np.ndarray,
        parent_objective_value: float,
        parent_constraint_value: float,
        discrete_indices: List[int],
        continuous_indices: List[int],
    ) -> Tuple[float, float]:
        pass

class LeadingOnes(ObjectiveBase):
    """
    (final class)
    """

    def __init__(self, ndims: int) -> None:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
    ) -> Tuple[float, float]:
        pass

class TrailingZeros(ObjectiveBase):
    """
    (final class)
    """

    def __init__(self, ndims: int) -> None:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
    ) -> Tuple[float, float]:
        pass

class HLeadingOnes(ObjectiveBase):
    """
    (final class)
    """

    def __init__(self, ndims: int, branching_factor: int = 2) -> None:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
    ) -> Tuple[float, float]:
        pass

class DeceptiveTrap(ObjectiveBase):
    """
    (final class)
    """

    @overload
    def __init__(self, ndims: int) -> None:
        pass

    @overload
    def __init__(self, ndims: int, block_size: int) -> None:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
    ) -> Tuple[float, float]:
        pass

class BimodalTrap(ObjectiveBase):
    """
    (final class)
    """

    @overload
    def __init__(self, ndims: int) -> None:
        pass

    @overload
    def __init__(self, ndims: int, block_size: int) -> None:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
    ) -> Tuple[float, float]:
        pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/bench/functions/continuous.h included by goblin.h                               //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_BENCH_FUNCTIONS_CONTINUOUS_H
#

class Sphere(ObjectiveBase):
    """
    (final class)
    """

    def __init__(self, ndims: int) -> None:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
    ) -> Tuple[float, float]:
        pass

    def evaluate_partial(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
        parent_discrete_values: np.ndarray,
        parent_continuous_values: np.ndarray,
        parent_discrete_active: np.ndarray,
        parent_continuous_active: np.ndarray,
        parent_objective_value: float,
        parent_constraint_value: float,
        discrete_indices: List[int],
        continuous_indices: List[int],
    ) -> Tuple[float, float]:
        pass

class Rosenbrock(ObjectiveBase):
    """
    (final class)
    """

    def __init__(self, ndims: int) -> None:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
    ) -> Tuple[float, float]:
        pass

class Rastrigin(ObjectiveBase):
    """
    (final class)
    """

    def __init__(self, ndims: int) -> None:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
    ) -> Tuple[float, float]:
        pass

class Griewank(ObjectiveBase):
    """
    (final class)
    """

    def __init__(self, ndims: int) -> None:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
    ) -> Tuple[float, float]:
        pass

class Ellipsoid(ObjectiveBase):
    """
    (final class)
    """

    def __init__(self, ndims: int) -> None:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
    ) -> Tuple[float, float]:
        pass

class CirclesInASquare(ObjectiveBase):
    """
    (final class)
    """

    def __init__(self, ndims: int) -> None:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
    ) -> Tuple[float, float]:
        pass

# TODO for ZDT problems: add domains & fronts/success criterion?

class ZDT1(MOFunctionBase):
    """/ ZDT1 from https://doi.org/10.1162/106365600568202"""

    def __init__(self, dims: int = 30) -> None:
        pass

    def num_objectives(self) -> int:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(self, solution: SolutionBase) -> None:
        pass

    def continuous_lower_bounds(self) -> np.ndarray:
        pass

    def continuous_upper_bounds(self) -> np.ndarray:
        pass

    def pareto_front(self, num_samples: int) -> ArchiveBase:
        pass

class ZDT2(MOFunctionBase):
    """/ ZDT2 from https://doi.org/10.1162/106365600568202"""

    def __init__(self, dims: int = 30) -> None:
        pass

    def num_objectives(self) -> int:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(self, solution: SolutionBase) -> None:
        pass

    def continuous_lower_bounds(self) -> np.ndarray:
        pass

    def continuous_upper_bounds(self) -> np.ndarray:
        pass

    def pareto_front(self, num_samples: int) -> ArchiveBase:
        pass

class ZDT3(MOFunctionBase):
    """/ ZDT3 from https://doi.org/10.1162/106365600568202"""

    def __init__(self, dims: int = 30) -> None:
        pass

    def num_objectives(self) -> int:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(self, solution: SolutionBase) -> None:
        pass

    def continuous_lower_bounds(self) -> np.ndarray:
        pass

    def continuous_upper_bounds(self) -> np.ndarray:
        pass

    def pareto_front(self, num_samples: int) -> ArchiveBase:
        pass

class ZDT4(MOFunctionBase):
    """/ ZDT4 from https://doi.org/10.1162/106365600568202"""

    def __init__(self, dims: int = 10) -> None:
        pass

    def num_objectives(self) -> int:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(self, solution: SolutionBase) -> None:
        pass

    def continuous_lower_bounds(self) -> np.ndarray:
        pass

    def continuous_upper_bounds(self) -> np.ndarray:
        pass

    def pareto_front(self, num_samples: int) -> ArchiveBase:
        pass

class ZDT5(MOFunctionBase):
    """/ ZDT5 from https://doi.org/10.1162/106365600568202"""

    def __init__(self, dims: int = 11) -> None:
        pass

    def num_objectives(self) -> int:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(self, solution: SolutionBase) -> None:
        pass

    def continuous_lower_bounds(self) -> np.ndarray:
        pass

    def continuous_upper_bounds(self) -> np.ndarray:
        pass

    def pareto_front(self, num_samples: int) -> ArchiveBase:
        pass

class ZDT6(MOFunctionBase):
    """/ ZDT6 from https://doi.org/10.1162/106365600568202"""

    def __init__(self, dims: int = 10) -> None:
        pass

    def num_objectives(self) -> int:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(self, solution: SolutionBase) -> None:
        pass

    def continuous_lower_bounds(self) -> np.ndarray:
        pass

    def continuous_upper_bounds(self) -> np.ndarray:
        pass

    def pareto_front(self, num_samples: int) -> ArchiveBase:
        pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/bench/functions/mixed.h included by goblin.h                                    //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_BENCH_FUNCTIONS_MIXED_H
#

class LeadingSpheres(ObjectiveBase):
    """
    (final class)
    """

    def __init__(self, ndims: int) -> None:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(
        self,
        discrete_values: np.ndarray,
        continuous_values: np.ndarray,
        discrete_active: np.ndarray,
        continuous_active: np.ndarray,
    ) -> Tuple[float, float]:
        pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/bench/problem.h included by goblin.h                                            //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_BENCH_PROBLEM_H
#

class PyFunctionBase(MOFunctionBase):
    def num_objectives(self) -> int:  # overridable (pure virtual)
        pass

    def num_discrete(self) -> int:  # overridable (pure virtual)
        pass

    def num_continuous(self) -> int:  # overridable (pure virtual)
        pass

    def eval(  # overridable (pure virtual)
        self, solution: SolutionBase
    ) -> Tuple[np.ndarray, float]:
        pass

    def evaluate(self, solution: SolutionBase) -> None:  # overridable
        pass

    def evaluate_partial(  # overridable
        self, solution: SolutionBase, parent: SolutionBase, subset: Subset
    ) -> None:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class Objectives(MOFunctionBase):
    """
    (final class)
    """

    def __init__(self, objectives: List[ObjectiveBase]) -> None:
        pass

    def num_objectives(self) -> int:
        pass

    def num_discrete(self) -> int:
        pass

    def num_continuous(self) -> int:
        pass

    def evaluate(self, solution: SolutionBase) -> None:
        pass

    def evaluate_partial(
        self, solution: SolutionBase, parent: SolutionBase, subset: Subset
    ) -> None:
        pass

class BenchmarkInstance(InstanceBase):
    """
    (final class)
    """

    def __init__(
        self,
        objectives: Union[List[ObjectiveBase], MOFunctionBase, ObjectiveBase],
        discrete_domain: Optional[Union[int, np.ndarray]] = None,
        continuous_lower_bound: Optional[Union[float, np.ndarray]] = None,
        continuous_upper_bound: Optional[Union[float, np.ndarray]] = None,
        continuous_init_lower_bound: Optional[Union[float, np.ndarray]] = None,
        continuous_init_upper_bound: Optional[Union[float, np.ndarray]] = None,
        init: Optional[AnyInit] = None,
        target_objectives: Optional[List[float]] = None,
        target_archive_size: Optional[int] = None,
    ) -> None:
        """Python bindings defaults:
        If any of the params below is None, then its default value below will be used:
            * discrete_domain: int(2)
            * continuous_lower_bound: -std.numeric_limits<float>.infinity()
            * continuous_upper_bound: std.numeric_limits<float>.infinity()
            * continuous_init_lower_bound: float(0.0)
            * continuous_init_upper_bound: float(1.0)
        """
        pass

    def set_init(self, init: AnyInit) -> None:
        pass

    def set_initial_bounds(
        self,
        continuous_init_lower_bound: Optional[Union[float, np.ndarray]] = None,
        continuous_init_upper_bound: Optional[Union[float, np.ndarray]] = None,
    ) -> None:
        """Python bindings defaults:
        If any of the params below is None, then its default value below will be used:
            * continuous_init_lower_bound: float(0.0)
            * continuous_init_upper_bound: float(1.0)
        """
        pass

    @overload
    def register_target_front(self, other: ArchiveBase) -> None:
        pass

    @overload
    def register_target_front(
        self, discrete: np.ndarray, continuous: np.ndarray
    ) -> None:
        pass

    def register_target_archive_size(self, target_archive_size: int) -> None:
        pass

    def discrete_domain_sizes(self) -> np.ndarray:
        pass

    def continuous_lower_bounds(self) -> np.ndarray:
        pass

    def continuous_upper_bounds(self) -> np.ndarray:
        pass

    def add_random(self, rng: Rng, solutions: SolutionSetBase, count: int) -> None:
        pass

    def continuous_init_lower_bounds(self) -> np.ndarray:
        pass

    def continuous_init_upper_bounds(self) -> np.ndarray:
        pass

    def evaluate(
        self, rng: Rng, solutions: SolutionSetBase, indices: List[int]
    ) -> None:
        pass

    def evaluate_partial(
        self,
        rng: Rng,
        solutions: SolutionSetBase,
        parents: SolutionSetBase,
        subsets: List[Subset],
        indices: List[int],
    ) -> None:
        pass

    def fitness(self) -> FitnessBase:
        pass

    def archive_fitness(self) -> ArchiveFitnessBase:
        pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/bench/timer.h included by goblin.h                                              //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_BENCH_TIMER_H
#

class Timer:
    def start(self) -> None:
        pass

    def stop(self) -> None:
        pass

    def elapsed(self) -> datetime.timedelta:
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/bench/tracked.h included by goblin.h                                            //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_BENCH_TRACKED_H
#

class TrackingOptions:
    @overload
    def __init__(
        self,
        logpath: pathlib.Path | str,
        log_info: Optional[List[Tuple[str, str]]] = None,
        archive_capacity: int = 100,
        max_evaluations_until_archive_adaption: int = 100000,
        consider_evaluation_time: bool = True,
        report_intermediate_results: bool = True,
        report_on_archive_change: bool = False,
        initial_evaluations_until_next_report: int = 10,
        eval_factor: int = 2,
        max_evaluations_until_next_report: int = 1000000,
        initial_generations_until_next_report: int = 1,
        generation_factor: int = 2,
        max_generations_until_next_report: int = 100,
        initial_time_until_next_report: Optional[datetime.timedelta] = None,
        time_factor: int = 2,
        max_time_until_next_report: Optional[datetime.timedelta] = None,
    ) -> None:
        """TODO at some point think about enabling dynamically setting the logging
         precision for floating points
         TODO at some point allow these params on Tracked::run to reduce the amount
         of config object nesting?


        Python bindings defaults:
            If any of the params below is None, then its default value below will be used:
                * initial_time_until_next_report: std.chrono.seconds(1)
                * max_time_until_next_report: std.chrono.minutes(10)
        """
        pass
    archive_capacity: int
    max_evaluations_until_archive_adaption: int
    consider_evaluation_time: bool
    report_intermediate_results: bool
    report_on_archive_change: bool

    initial_evaluations_until_next_report: int
    eval_factor: int  # 1 is linear, >= 2 is exponential spacing
    max_evaluations_until_next_report: int

    initial_generations_until_next_report: int
    generation_factor: int  # 1 is linear, >= 2 is exponential spacing
    max_generations_until_next_report: int

    initial_time_until_next_report: datetime.timedelta
    time_factor: int  # 1 is linear, >= 2 is exponential spacing
    max_time_until_next_report: datetime.timedelta

class Tracked(WrappedInstance):
    """/ An instance that intercepts evaluations
    (final class)
    """

    def evaluate(
        self, rng: Rng, solutions: SolutionSetBase, indices: List[int]
    ) -> None:
        pass

    def evaluate_partial(
        self,
        rng: Rng,
        solutions: SolutionSetBase,
        parents: SolutionSetBase,
        subsets: List[Subset],
        indices: List[int],
    ) -> None:
        pass

    def adapt(self, rng: Rng) -> bool:
        pass

    def gradients(
        self,
        rng: Rng,
        solutions: SolutionSetBase,
        parents: SolutionSetBase,
        subsets: List[Subset],
        indices: List[int],
        evaluations: int,
    ) -> np.ndarray:
        pass

    def gradient_steps(
        self,
        rng: Rng,
        solutions: SolutionSetBase,
        parents: SolutionSetBase,
        indices: List[int],
        num_steps: int,
    ) -> Tuple[List[int], int]:
        pass

    @staticmethod
    def run(
        instance: InstanceBase,
        method: MethodBase,
        budget: Budget,
        config: TrackingOptions,
        seed: Optional[int] = None,
        population_size: Optional[int] = None,
    ) -> Tuple[ArchiveBase, TerminationStatus]:
        pass

def debug_log(
    problem: InstanceBase,
    path: str,
    headers: str = "",
    values: str = "",
    population: Optional[SolutionSetBase] = None,
) -> None:
    """/ Tracked running was intended to unify reporting across algorithms
    / - this method abuses that functionality to re-use that logging for
    / other purposes controlled by the algorithm, not the tracking
    """
    pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/methods/ims.h included by goblin.h                                              //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_LIB_IMS_H
#

class IMSOptions:
    initial_population_size: int = 2
    max_num_populations: int = 25
    subgeneration_factor: int = 4
    restart_stale_populations: bool = False
    stop_covered_populations: bool = False
    initial_num_clusters: int = 1
    archive_capacity: Optional[int] = 100
    so_parameter_space_clustering: bool = (
        False  # TODO remove or implement parameter space clustering
    )
    additional_clusters_per_start: int = 1
    generations_without_improvement_until_restart: Optional[int] = None

    reevaluate_solutions_after_adaption: bool = True

    population_logfile: Optional[str] = None
    population_log_resolution: str = "archive"
    def __init__(
        self,
        initial_population_size: int = 2,
        max_num_populations: int = 25,
        subgeneration_factor: int = 4,
        restart_stale_populations: bool = False,
        stop_covered_populations: bool = False,
        initial_num_clusters: int = 1,
        archive_capacity: Optional[int] = 100,
        so_parameter_space_clustering: bool = False,
        additional_clusters_per_start: int = 1,
        generations_without_improvement_until_restart: Optional[int] = None,
        reevaluate_solutions_after_adaption: bool = True,
        population_logfile: Optional[str] = None,
        population_log_resolution: str = "archive",
    ) -> None:
        """Auto-generated default constructor with named params"""
        pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/methods/amalgam.h included by goblin.h                                          //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_AMALGAM_H
#

class AMaLGaM(MethodBase):
    """
    (final class)
    """

    def __init__(
        self,
        init_lower_bound: float = -1.0,
        init_upper_bound: float = 1.0,
        fitness_variance_threshold: float = 0.0,
        max_num_populations: int = 25,
        max_evaluations: int = 1000000,
        target_value: Optional[float] = None,
        base_population_size: Optional[int] = None,
        seed: Optional[int] = None,
    ) -> None:
        pass

    def set_init_bounds(self, lower: float, upper: float) -> None:
        pass

    @overload
    def run(
        self,
        function: Callable[[np.ndarray], Tuple[float, float]],
        bounds: List[Tuple[float, float]],
    ) -> Tuple[np.ndarray, float, float]:
        pass

    def current_generation(self) -> Optional[int]:
        pass

    @overload
    def run(
        self,
        problem: InstanceBase,
        budget: Budget,
        seed: Optional[int] = None,
        population_size: Optional[int] = None,
    ) -> Tuple[ArchiveBase, TerminationStatus]:
        pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/methods/library.h included by goblin.h                                          //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_GOMEA_LIBRARY_H
#

# Doesn't work yet since we store the full class, not a pointer...
# / forward declaration to avoid pulling in the library headers in the header
# namespace gomea {
# struct linkage_config_t;

# namespace discrete {
# struct Config;
# struct gomeaIMS;
# };  // namespace discrete

# namespace realvalued {
# struct Config;
# struct rvg_t;
# };  // namespace realvalued
# };  // namespace gomea

class DiscreteGOMEA(MethodBase):
    """
    (final class)
    """

    def __init__(
        self,
        linkage_model: str = "LinkageTree",
        similarity_metric: str = "MI",
        gene_invariant: bool = False,
        forced_improvements: bool = True,
        filter_linkage: bool = False,
        max_subset_size: Optional[int] = None,
        base_population_size: int = 2,
        max_number_of_populations: int = 100,
        subgeneration_factor: int = 4,
        max_archive_size: int = 0,
        fos_order: str = "default",
    ) -> None:
        pass

    def run(
        self,
        problem: InstanceBase,
        budget: Budget,
        seed: Optional[int] = None,
        population_size: Optional[int] = None,
    ) -> Tuple[ArchiveBase, TerminationStatus]:
        pass

    def current_generation(self) -> Optional[int]:
        pass

class RvGOMEA(MethodBase):
    """
    (final class)
    """

    def __init__(
        self,
        linkage_model: str = "Full",
        base_population_size: int = 10,
        max_number_of_populations: int = 25,
        subgeneration_factor: int = 8,
        max_nis: int = 100,
        selection_percentile: float = 0.35,
        distribution_multiplier_decrease: float = 0.9,
        standard_deviation_threshold: float = 1.0,
        fitness_variance_tolerance: float = 0.0,
        selection_during_gom: bool = True,
        update_elitist_during_gom: bool = True,
    ) -> None:
        pass

    def run(
        self,
        problem: InstanceBase,
        budget: Budget,
        seed: Optional[int] = None,
        population_size: Optional[int] = None,
    ) -> Tuple[ArchiveBase, TerminationStatus]:
        pass

    def current_generation(self) -> Optional[int]:
        pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/methods/mo_binary.h included by goblin.h                                        //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_MO_BINARY_GOMEA_H
#

class MOBinaryGOMEA(MethodBase):
    """
    (final class)
    """

    def __init__(
        self,
        initial_population_size: int = 8,
        max_num_populations: int = 25,
        subgeneration_factor: int = 2,
        target_archive_size: int = 100,
        mutation: str = "none",
        stop_covered_populations: bool = False,
    ) -> None:
        pass

    def current_generation(self) -> Optional[int]:
        pass

    def run(
        self,
        problem: InstanceBase,
        budget: Budget,
        seed: Optional[int] = None,
        population_size: Optional[int] = None,
    ) -> Tuple[ArchiveBase, TerminationStatus]:
        pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/methods/mo_rv_gomea.h included by goblin.h                                      //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_MO_RV_GOMEA_H
#

class MoRvGOMEA(MethodBase):
    """
    (final class)
    """

    def __init__(
        self,
        initial_population_size: Optional[int] = None,
        max_num_populations: int = 25,
        subgeneration_factor: int = 8,
        target_archive_size: int = 100,
        selection_percentile: float = 0.35,
        distribution_multiplier_decrease: float = 0.9,
        std_deviation_ratio_threshold: float = 1.0,
        max_no_improvement_stretch: Optional[int] = None,
        initial_num_clusters: Optional[int] = None,
        linkage_model: Union[str, StaticFOS] = "LinkageTree",
        max_subset_size: Optional[int] = None,
        boundary_repair: bool = False,
        forced_improvements: bool = False,
        partial_evaluations: bool = True,
    ) -> None:
        pass

    def current_generation(self) -> Optional[int]:
        pass

    def run(
        self,
        problem: InstanceBase,
        budget: Budget,
        seed: Optional[int] = None,
        population_size: Optional[int] = None,
    ) -> Tuple[ArchiveBase, TerminationStatus]:
        pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/methods/mixed.h included by goblin.h                                            //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_MIXED_GOMEA_H
#

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/methods/continuous.h included by goblin/methods/mixed.h                         //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_METHODS_CONTINUOUS_H
#

def estimate_mean(
    solutions: SolutionSetBase,
    active_indices: List[int],
    active_counts: List[int],
    selection_percentile: float,
    indices: List[int],
    intron_aware: bool,
) -> np.ndarray:
    pass

def estimate_cov(
    solutions: SolutionSetBase,
    active_indices: List[int],
    active_counts: List[int],
    selection_percentile: float,
    mean: np.ndarray,
    indices: List[int],
    intron_aware: bool,
) -> np.ndarray:
    pass

class RvSubsetStateBase:
    distribution_multiplier: float = 1.0

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class RvSamplingModelBase:
    """/ Separate sampling models roughly as per https://ir.cwi.nl/pub/30344/30344.pdf
    / TODO ask anton about API - feels still a bit crude...
    """

    def init(self, subset: Subset) -> RvSubsetStateBase:  # overridable (pure virtual)
        pass

    def inherit(  # overridable (pure virtual)
        self, subset: Subset, previous_subset: Subset, previous_state: RvSubsetStateBase
    ) -> RvSubsetStateBase:
        pass

    def update(  # overridable (pure virtual)
        self,
        solutions: SolutionSetBase,
        by_fitness_decreasing: List[int],
        active_counts: List[int],
        intron_aware: bool,
        subset: Subset,
        selection_percentile: float,
        state: RvSubsetStateBase,
    ) -> None:
        pass

    def sample(  # overridable (pure virtual)
        self, rng: Rng, state: RvSubsetStateBase
    ) -> np.ndarray:
        pass

    def adapt(  # overridable
        self,
        solutions: SolutionSetBase,
        improved_indices: List[int],
        oob_ratio: float,
        max_nis_reached: bool,
        intron_aware: bool,
        subset: Subset,
        state: RvSubsetStateBase,
    ) -> None:
        pass

    def converged(self, state: RvSubsetStateBase) -> bool:  # overridable (pure virtual)
        pass

    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class AMaLGaMSubsetState(RvSubsetStateBase):
    """
    (final class)
    """

    enable_smoothing: bool = False
    mean: np.ndarray
    cov: (
        np.ndarray
    )  # for storing the unscaled covariance, since in the incremental version the scaling happens after the
    # smoothing:
    # https://github.com/renzoscholman/irv-gomea/blob/89c62cf007858bd8ab9c2ba7955c7b80121/src/distribution.cpp#L510
    L: np.ndarray
    def __init__(self) -> None:
        """Autogenerated default constructor"""
        pass

class AMaLGaMSamplingModel(RvSamplingModelBase):
    """
    (final class)
    """

    def __init__(
        self,
        use_mahalanobis_distance_for_sdr: bool = False,
        eta_cov: float = 1.0,
        std_deviation_ratio_threshold: float = 1.0,
        distribution_multiplier_decrease: float = 0.9,
        distribution_multiplier_increase: Optional[float] = None,
        min_distribution_multiplier: float = 1e-10,
        num_cholesky_tries: int = 1,
    ) -> None:
        """Python bindings defaults:
        If distribution_multiplier_increase is None, then its default value will be: 1.0 / 0.9
        """
        pass

    def init(self, subset: Subset) -> RvSubsetStateBase:
        pass

    def inherit(
        self, subset: Subset, previous_subset: Subset, previous_state: RvSubsetStateBase
    ) -> RvSubsetStateBase:
        pass

    def update(
        self,
        solutions: SolutionSetBase,
        by_fitness_decreasing: List[int],
        active_counts: List[int],
        intron_aware: bool,
        subset: Subset,
        selection_percentile: float,
        state: RvSubsetStateBase,
    ) -> None:
        pass

    def sample(self, rng: Rng, state: RvSubsetStateBase) -> np.ndarray:
        pass

    def adapt(
        self,
        solutions: SolutionSetBase,
        improved_indices: List[int],
        oob_ratio: float,
        max_nis_reached: bool,
        intron_aware: bool,
        subset: Subset,
        state: RvSubsetStateBase,
    ) -> None:
        pass

    def converged(self, state: RvSubsetStateBase) -> bool:
        pass

class RvOptions:
    enabled: bool = True
    intron_aware: bool = False
    intron_aware_intermediate_updates: bool = False
    intron_aware_mean_estimation: bool = False
    intron_aware_cov_estimation: bool = False
    intron_aware_ams: bool = False

    selection_percentile: float = 0.35
    p_accept: float = 0.05

    init_ams_from_population_mean: bool = True
    # If randomized, the AMS indices are randomly picked from all active solutions.
    # Otherwise the first `floor(selection_percentile * 0.5)` active solutions are used.
    randomize_ams_indices: bool = False
    enable_partial_ams: bool = True
    enable_full_ams: bool = True
    delta_ams: float = 2.0
    eta_ams: float = 1.0

    max_nis: int = 100
    num_forced_improvement_tries: int = (
        8  # 8 is the RV GOMEA default if I did not miscalculate (1.0 / 2^8 < 0.01)
    )

    # In the GBO setting with partial evaluations, numerical errors of partial fitness updates
    # can accumulate and it might be needed to perform full evaluations once in a while
    #
    # In that case, the default number of generations until re-evaluation is `50`
    #
    # Note that in this setting all archive solutions should be re-evaluated in case the target seems to have been
    # reached to ensure that this is not due to numeric errors. This does not happen in this version.
    generations_until_full_evaluation: Optional[int] = None

    population_logfile: Optional[str] = None
    selection_logfile: Optional[str] = None
    subset_logfile: Optional[str] = None
    sample_logfile: Optional[str] = None

    check_synched_archives: bool = False

    def validate(self) -> None:
        pass

    def __init__(
        self,
        enabled: bool = True,
        intron_aware: bool = False,
        intron_aware_intermediate_updates: bool = False,
        intron_aware_mean_estimation: bool = False,
        intron_aware_cov_estimation: bool = False,
        intron_aware_ams: bool = False,
        selection_percentile: float = 0.35,
        p_accept: float = 0.05,
        init_ams_from_population_mean: bool = True,
        randomize_ams_indices: bool = False,
        enable_partial_ams: bool = True,
        enable_full_ams: bool = True,
        delta_ams: float = 2.0,
        eta_ams: float = 1.0,
        max_nis: int = 100,
        num_forced_improvement_tries: int = 8,
        generations_until_full_evaluation: Optional[int] = None,
        population_logfile: Optional[str] = None,
        selection_logfile: Optional[str] = None,
        subset_logfile: Optional[str] = None,
        sample_logfile: Optional[str] = None,
        check_synched_archives: bool = False,
    ) -> None:
        """Auto-generated default constructor with named params"""
        pass

def sort_by_quality_decreasing(
    fitness: FitnessBase,
    solutions: SolutionSetBase,
    indices: List[int],
    objective: Optional[int],
) -> List[int]:
    pass

class RvState:
    def __init__(
        self,
        options: RvOptions,
        linkage_model: LinkageModelBase,
        sampling_model: RvSamplingModelBase,
    ) -> None:
        pass

    def perform_generation(
        self,
        rng: Rng,
        archive: ArchiveBase,
        problem: InstanceBase,
        solutions: SolutionSetBase,
        parents: SolutionSetBase,
        solution_clusters: List[int],
        cluster_solutions: List[List[int]],
    ) -> int:
        pass

    def converged(self) -> bool:
        pass
    # private:

    def select_and_learn_linkage(
        self,
        rng: Rng,
        archive: ArchiveBase,
        problem: InstanceBase,
        solutions: SolutionSetBase,
        cluster_solutions: List[List[int]],
    ) -> List[Set[int]]:
        pass

    def should_accept(
        self,
        rng: Rng,
        fitness: FitnessBase,
        archive: ArchiveBase,
        solution: SolutionBase,
        parent: SolutionBase,
        objective: Optional[int],
        strict: bool,
    ) -> Tuple[bool, bool, bool]:
        pass

    def gom_step(
        self,
        rng: Rng,
        archive: ArchiveBase,
        problem: InstanceBase,
        solutions: SolutionSetBase,
        parents: SolutionSetBase,
        solution_clusters: List[int],
        cluster_solutions: List[List[int]],
        subset_orders: np.ndarray,
        subset_idx: int,
        ams_indices: List[Set[int]],
    ) -> int:
        pass

    def full_ams(
        self,
        rng: Rng,
        archive: ArchiveBase,
        problem: InstanceBase,
        solutions: SolutionSetBase,
        parents: SolutionSetBase,
        solution_clusters: List[int],
        ams_indices: List[Set[int]],
    ) -> int:
        pass

    def forced_improvements(
        self,
        rng: Rng,
        archive: ArchiveBase,
        problem: InstanceBase,
        solutions: SolutionSetBase,
        parents: SolutionSetBase,
        solution_clusters: List[int],
        subset_orders: np.ndarray,
        max_num_subsets: int,
    ) -> int:
        pass
    # std::tuple<std::shared_ptr<ArchiveBase>, TerminationStatus>
    # run(InstanceBase &problem, const Budget &budget, std::optional<usize> seed,
    #     std::optional<usize> population_size){
    #       // ims go brr
    #     }

    def current_generation(self) -> Optional[int]:
        """// override final"""
        pass
    options: RvOptions

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/methods/mixed.h continued                                                       //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////

def create_and_register_clusters(
    rng: Rng,
    archive: ArchiveBase,
    fitness: FitnessBase,
    solutions: SolutionSetBase,
    num_clusters: int,
    donor_pool_size: int,
    previous_solutions: SolutionSetBase,
    previous_clusters: List[int],
) -> Tuple[List[int], List[List[int]], List[List[int]]]:
    pass

class FosStats:
    solution_activation_rate: List[
        float
    ]  # whats the proportion of solutions where initially at least one of the
    # variables in the subset is active?
    variables_activation_rate: List[
        float
    ]  # conditioned on solutions with at least one active variables in the subset, whats
    # the proportion of variables in the subset that are active on average?
    usage_count: List[
        int
    ]  # how often was the FOS used? (without FI, - should be the population size)
    evaluation_count: List[
        int
    ]  # how often was an evaluation needed? (i.e. active parts were modified)
    acceptance_count: List[int]  # how often was the change accepted? (after evaluation)
    cumulative_fitness_difference: List[
        float
    ]  # how big were the accepted improvements?
    finite_acceptance_count: List[
        int
    ]  # how many improvements had a finite fitness difference to their parent?
    # (inf/nan mess up the average...)
    similarity: np.ndarray
    def __init__(
        self,
        solution_activation_rate: Optional[List[float]] = None,
        variables_activation_rate: Optional[List[float]] = None,
        usage_count: Optional[List[int]] = None,
        evaluation_count: Optional[List[int]] = None,
        acceptance_count: Optional[List[int]] = None,
        cumulative_fitness_difference: Optional[List[float]] = None,
        finite_acceptance_count: Optional[List[int]] = None,
        similarity: Optional[np.ndarray] = None,
    ) -> None:
        """Auto-generated default constructor with named params


        Python bindings defaults:
            If any of the params below is None, then its default value below will be used:
                * solution_activation_rate: List[float]()
                * variables_activation_rate: List[float]()
                * usage_count: List[int]()
                * evaluation_count: List[int]()
                * acceptance_count: List[int]()
                * cumulative_fitness_difference: List[float]()
                * finite_acceptance_count: List[int]()
                * similarity: Mat<float>()
        """
        pass

class PopulationOptions:
    donor_pool_size_multiplier: float = 2.0
    max_nis: Optional[int] = None
    forced_improvements: bool = True
    enable_mixed_forced_improvements: bool = True
    target_continuous_to_discrete_balance: float = 1.0
    sequential_gom: bool = (
        False  # performs GOM sequentially per solution, incompatible with other mechanisms
    )
    strict_elite_acceptance: bool = (
        False  # should the single objective elite solutions accept only strict improvements or also neutral changes?
    )

    donor_search_proportion: float = (
        0.0  # the fraction of solutions to consider before skipping an evaluation in case
    )
    # of all subset variables being identical between the solution and donor
    subset_logfile: Optional[str] = None
    intermediate_population_logfile: Optional[str] = None
    generation: int = 0
    initial_generations_until_next_fos_log: int = (
        5  # > 0, subset stats are logged every
    )
    fos_log_factor: int = 2  # 1 is linear, 2 is exponential log spacing

    # Coefficient mutation as per https://doi.org/10.1145/3520304.3534036
    continuous_mutation_probability: float = 0.0
    continuous_mutation_temperature: float = 0.1
    continuous_mutation_decay_factor: float = 0.9
    continuous_mutation_decay_patience: Optional[int] = 5

    mutate_before_gradient_step: bool = True
    gradient_step_frequency: int = 0
    gradient_step_count: int = 10

    # If std::nullopt, then perform strong mutation (i.e. 1/subset size).
    # Note that for single element subsets, this collapses to random search
    discrete_mutation_probability: Optional[float] = 0.0

    gene_invariant: bool = False

    # TODO this whole file really needs a refactor lol
    use_fancy_gpu_gp_gom: bool = False
    resample_inactive_donor_values: bool = False
    def __init__(
        self,
        donor_pool_size_multiplier: float = 2.0,
        max_nis: Optional[int] = None,
        forced_improvements: bool = True,
        enable_mixed_forced_improvements: bool = True,
        target_continuous_to_discrete_balance: float = 1.0,
        sequential_gom: bool = False,
        strict_elite_acceptance: bool = False,
        donor_search_proportion: float = 0.0,
        subset_logfile: Optional[str] = None,
        intermediate_population_logfile: Optional[str] = None,
        generation: int = 0,
        initial_generations_until_next_fos_log: int = 5,
        fos_log_factor: int = 2,
        continuous_mutation_probability: float = 0.0,
        continuous_mutation_temperature: float = 0.1,
        continuous_mutation_decay_factor: float = 0.9,
        continuous_mutation_decay_patience: Optional[int] = 5,
        mutate_before_gradient_step: bool = True,
        gradient_step_frequency: int = 0,
        gradient_step_count: int = 10,
        discrete_mutation_probability: Optional[float] = 0.0,
        gene_invariant: bool = False,
        use_fancy_gpu_gp_gom: bool = False,
        resample_inactive_donor_values: bool = False,
    ) -> None:
        """Auto-generated default constructor with named params"""
        pass

class MixedGOMEA(MethodBase):
    def __init__(
        self,
        population_options: Optional[PopulationOptions] = None,
        rv_options: Optional[RvOptions] = None,
        ims_options: Optional[IMSOptions] = None,
        discrete_model: Optional[LinkageModelBase] = None,
        continuous_model: Optional[LinkageModelBase] = None,
        sampling_model: Optional[RvSamplingModelBase] = None,
        repr: str = "aos",
    ) -> None:
        """Python bindings defaults:
        If any of the params below is None, then its default value below will be used:
            * population_options: PopulationOptions()
            * rv_options: RvOptions()
            * ims_options: IMSOptions()
            * discrete_model: std.make_shared<LinkageTreeFOS>()
            * continuous_model: std.make_shared<FullFOS>()
            * sampling_model: std.make_shared<AMaLGaMSamplingModel>()
        """
        pass

    def run(
        self,
        problem: InstanceBase,
        budget: Budget,
        seed: Optional[int] = None,
        population_size: Optional[int] = None,
    ) -> Tuple[ArchiveBase, TerminationStatus]:
        pass

    def current_generation(self) -> Optional[int]:
        pass

    def current_population(self) -> Optional[Tuple[int, int, int]]:
        pass

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/methods/classic/common.h included by goblin.h                                   //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_CLASSIC_COMMON_H
#

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/methods/classic/de.h included by goblin.h                                       //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_CLASSIC_DE_H
#

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/methods/classic/es.h included by goblin.h                                       //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_ES_H
#

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/methods/classic/pso.h included by goblin.h                                      //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_CLASSIC_PSO_H
#

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/methods/classic/simple_ga.h included by goblin.h                                //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_SIMPLE_GA_H
#

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/methods/classic/standard_gp.h included by goblin.h                              //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_STANDARD_GP_H
#

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin/examples/voronoi.h included by goblin.h                                         //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////
# #ifndef _GOBLIN_EXAMPLES_VORONOI_H
#

# #endif

# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#                       goblin.h continued                                                                     //
# //////////////////////////////////////////////////////////////////////////////////////////////////////////////

# clang-format on

# #endif

# <submodule classic>
class classic:  # Proxy class that introduces typings for the *submodule* classic
    pass  # (This corresponds to a C++ namespace. All methods are static!)

    class SelectionStrategyBase:
        def select(  # overridable (pure virtual)
            self,
            rng: Rng,
            fitness: FitnessBase,
            solutions: SolutionSetBase,
            target_size: int,
        ) -> List[int]:
            pass

        def __init__(self) -> None:
            """Autogenerated default constructor"""
            pass

    class TournamentSelection(SelectionStrategyBase):
        def __init__(
            self, tournament_size: int = 2, with_replacement: bool = False
        ) -> None:
            pass

        def select(
            self,
            rng: Rng,
            fitness: FitnessBase,
            solutions: SolutionSetBase,
            target_size: int,
        ) -> List[int]:
            pass

    class TruncationSelection(SelectionStrategyBase):
        def select(
            self,
            rng: Rng,
            fitness: FitnessBase,
            solutions: SolutionSetBase,
            target_size: int,
        ) -> List[int]:
            pass

        def __init__(self) -> None:
            """Autogenerated default constructor"""
            pass

    class EABase(MethodBase):
        @overload
        def __init__(self, population_size: int) -> None:
            pass

        def step(  # overridable (pure virtual)
            self,
            rng: Rng,
            problem: InstanceBase,
            population: SolutionSetBase,
            archive: ArchiveBase,
        ) -> int:
            pass

        def set_population(self, population: SolutionSetBase) -> None:
            pass

        def get_population(self) -> AoSSet:
            pass

        def run(  # overridable
            self,
            problem: InstanceBase,
            budget: Budget,
            seed: Optional[int] = None,
            population_size: Optional[int] = None,
        ) -> Tuple[ArchiveBase, TerminationStatus]:
            pass

        def current_generation(self) -> Optional[int]:  # overridable
            pass

        def current_population(self) -> Optional[Tuple[int, int, int]]:  # overridable
            pass

    class DEStrategyBase:
        def trial_vector(  # overridable (pure virtual)
            self,
            rng: Rng,
            problem: InstanceBase,
            population: SolutionSetBase,
            archive: ArchiveBase,
            idx: int,
            subset: Subset,
        ) -> Tuple[Solution, bool]:
            pass

        def __init__(self) -> None:
            """Autogenerated default constructor"""
            pass

    class Rand1Bin(DEStrategyBase):
        def __init__(
            self,
            F: float = 0.8,
            Cr: float = 0.9,
            base: str = "best",
            scale: str = "dither",
        ) -> None:
            pass

        def trial_vector(
            self,
            rng: Rng,
            problem: InstanceBase,
            population: SolutionSetBase,
            archive: ArchiveBase,
            idx: int,
            subset: Subset,
        ) -> Tuple[Solution, bool]:
            pass

    class DE(EABase):
        def __init__(
            self, population_size: int = 100, strategy: Optional[DEStrategyBase] = None
        ) -> None:
            """Python bindings defaults:
            If strategy is None, then its default value will be: std.make_shared<Rand1Bin>()
            """
            pass

        def step(
            self,
            rng: Rng,
            problem: InstanceBase,
            population: SolutionSetBase,
            archive: ArchiveBase,
        ) -> int:
            pass

    class ESStrategy(enum.IntEnum):
        # / Single variance for all variables
        SingleVariance = enum.auto()  # (= 0)
        # / Separate variance for all variables
        MultipleVariance = enum.auto()  # (= 1)
        # / Full covariance matrix
        FullVariance = enum.auto()  # (= 2)
        # / Directional variance for one arbitrary direction, single variance in all other directions
        DirectedVariance = enum.auto()  # (= 3)

    class ESStrategyParameters:
        strategy: ESStrategy
        parameters: np.ndarray

        def clone(self) -> SolutionExtensionBase:
            pass

        def __init__(self) -> None:
            """Autogenerated default constructor"""
            pass

    class ES(EABase):
        def __init__(
            self,
            population_size: int = 8,
            num_parents: int = 1,
            num_offspring: int = 50,
            steady_state: bool = True,
            strategy: str = "single",
            epsilon: float = 1e-6,
            tau: Optional[float] = None,
            tau_i: Optional[float] = None,
            beta: float = 0.0873,
        ) -> None:
            pass

        def step(
            self,
            rng: Rng,
            problem: InstanceBase,
            population: SolutionSetBase,
            archive: ArchiveBase,
        ) -> int:
            pass

    class PSOTopologyBase:
        def neighbours(  # overridable (pure virtual)
            self, rng: Rng, population: SolutionSetBase, idx: int
        ) -> List[int]:
            pass

        def __init__(self) -> None:
            """Autogenerated default constructor"""
            pass

    class RingTopology(PSOTopologyBase):
        def __init__(self, num_neighbours: int = 2) -> None:
            """/ Considers a population of size N as ring where the ends wrap (nodes 0 and N-1 are neighbours). The number of
            / neighbours determines the (symmetric) reach of each node around the ring, where `num_neighbours >= floor(N-1) / 2`
            / corresponds to a fully connected star topology.
            """
            pass

        def neighbours(
            self, rng: Rng, population: SolutionSetBase, idx: int
        ) -> List[int]:
            pass

    class PSOState:
        velocity: np.ndarray
        previous_best: np.ndarray

        @overload
        def __init__(self) -> None:
            pass

        @overload
        def __init__(self, other: PSOState) -> None:
            pass

        @overload
        def __init__(self, other: PSOState) -> None:
            pass

        def clone(self) -> SolutionExtensionBase:
            pass

        def previous_best_quality(self) -> QualityBase: ...

    class PSO(EABase):
        def __init__(
            self,
            population_size: int = 25,
            inertia: float = 0.729,
            cognitive: float = 1.494,
            social: float = 1.494,
            topology: Optional[PSOTopologyBase] = None,
        ) -> None:
            """Python bindings defaults:
            If topology is None, then its default value will be: std.make_shared<RingTopology>()
            """
            pass

        def step(
            self,
            rng: Rng,
            problem: InstanceBase,
            population: SolutionSetBase,
            archive: ArchiveBase,
        ) -> int:
            pass

    class DiscreteCrossoverBase:
        """/ Strategy used to generate the crossover masks to exchange information between two parents"""

        def crossover_mask(  # overridable
            self,
            rng: Rng,
            problem: InstanceBase,
            donor: SolutionBase,
            offspring: SolutionBase,
        ) -> Subset:
            pass

        def crossover(  # overridable
            self,
            rng: Rng,
            problem: InstanceBase,
            donor: SolutionBase,
            offspring: SolutionBase,
        ) -> bool:
            pass

        def __init__(self) -> None:
            """Autogenerated default constructor"""
            pass

    class CombinedCrossover(DiscreteCrossoverBase):
        @overload
        def __init__(
            self,
            operators: List[Tuple[DiscreteCrossoverBase, float]],
            normalize: bool = True,
        ) -> None:
            pass

        def crossover(
            self,
            rng: Rng,
            problem: InstanceBase,
            donor: SolutionBase,
            offspring: SolutionBase,
        ) -> bool:
            pass

    class UniformCrossover(DiscreteCrossoverBase):
        def __init__(self, p_crossover: float = 0.5) -> None:
            pass

        def crossover_mask(
            self,
            rng: Rng,
            problem: InstanceBase,
            donor: SolutionBase,
            offspring: SolutionBase,
        ) -> Subset:
            pass

    class NPointCrossover(DiscreteCrossoverBase):
        def __init__(self, num_points: int = 1) -> None:
            pass

        def crossover_mask(
            self,
            rng: Rng,
            problem: InstanceBase,
            donor: SolutionBase,
            offspring: SolutionBase,
        ) -> Subset:
            pass

    class DiscreteMutationBase:
        def mutate(  # overridable (pure virtual)
            self, rng: Rng, problem: InstanceBase, offspring: SolutionBase
        ) -> None:
            pass

        def __init__(self) -> None:
            """Autogenerated default constructor"""
            pass

    class CombinedMutation(DiscreteMutationBase):
        @overload
        def __init__(
            self,
            operators: List[Tuple[DiscreteMutationBase, float]],
            normalize: bool = True,
        ) -> None:
            pass

        def mutate(
            self, rng: Rng, problem: InstanceBase, offspring: SolutionBase
        ) -> None:
            pass

    class RandomMutation(DiscreteMutationBase):
        def __init__(self, p_mutation: Optional[float] = None) -> None:
            pass

        def mutate(
            self, rng: Rng, problem: InstanceBase, offspring: SolutionBase
        ) -> None:
            pass

    class LocalizedMutation(DiscreteMutationBase):
        """/ A mutation operator that assumes an ordinal relationship for discrete variables"""

        def __init__(
            self,
            p_mutation: Optional[float] = None,
            strength: float = 0.05,
            wrap: bool = False,
        ) -> None:
            pass

        def mutate(
            self, rng: Rng, problem: InstanceBase, offspring: SolutionBase
        ) -> None:
            pass

    class SimpleGA(EABase):
        def __init__(
            self,
            population_size: int = 100,
            crossover: Optional[DiscreteCrossoverBase] = None,
            mutation: Optional[DiscreteMutationBase] = None,
            steady_state: bool = True,
            selection: Optional[SelectionStrategyBase] = None,
        ) -> None:
            """Python bindings defaults:
            If any of the params below is None, then its default value below will be used:
                * crossover: std.make_shared<UniformCrossover>()
                * mutation: std.make_shared<RandomMutation>()
                * selection: std.make_shared<TournamentSelection>(4)
            """
            pass

        def check_changes(
            self,
            parent: SolutionBase,
            offspring: SolutionBase,
            changed_indices: List[int],
            evaluation_needed: bool,
        ) -> None:
            pass

        def step(
            self,
            rng: Rng,
            problem: InstanceBase,
            population: SolutionSetBase,
            archive: ArchiveBase,
        ) -> int:
            pass

    class GPVariationOperatorBase:
        def apply(  # overridable (pure virtual)
            self,
            rng: Rng,
            problem: InstanceBase,
            ctx: GPContext,
            population: SolutionSetBase,
            offspring: SolutionBase,
        ) -> None:
            pass

        def __init__(self) -> None:
            """Autogenerated default constructor"""
            pass

    class Chained(GPVariationOperatorBase):
        @overload
        def __init__(
            self, operators: List[Tuple[GPVariationOperatorBase, float]]
        ) -> None:
            pass

        def apply(
            self,
            rng: Rng,
            problem: InstanceBase,
            ctx: GPContext,
            population: SolutionSetBase,
            offspring: SolutionBase,
        ) -> None:
            pass

    class SubtreeCrossover(GPVariationOperatorBase):
        def apply(
            self,
            rng: Rng,
            problem: InstanceBase,
            ctx: GPContext,
            population: SolutionSetBase,
            offspring: SolutionBase,
        ) -> None:
            pass

        def __init__(self) -> None:
            """Autogenerated default constructor"""
            pass

    class SubtreeMutation(GPVariationOperatorBase):
        """/ Replaces random subtree with a random subtree"""

        def apply(
            self,
            rng: Rng,
            problem: InstanceBase,
            ctx: GPContext,
            population: SolutionSetBase,
            offspring: SolutionBase,
        ) -> None:
            pass

        def __init__(self) -> None:
            """Autogenerated default constructor"""
            pass

    class ConstantMutation(GPVariationOperatorBase):
        """/ Randomly mutates constants"""

        def __init__(
            self, probability: float = 0.25, temperature: float = 0.25
        ) -> None:
            pass

        def apply(
            self,
            rng: Rng,
            problem: InstanceBase,
            ctx: GPContext,
            population: SolutionSetBase,
            offspring: SolutionBase,
        ) -> None:
            pass

    class StandardGP(EABase):
        def __init__(
            self,
            population_size: int = 32,
            steady_state: bool = True,
            variation_operator: Optional[GPVariationOperatorBase] = None,
            selection_strategy: Optional[SelectionStrategyBase] = None,
        ) -> None:
            """Python bindings defaults:
            If any of the params below is None, then its default value below will be used:
                * variation_operator: GPVariationOperatorBase()
                * selection_strategy: std.make_shared<TournamentSelection>(2)
            """
            pass

        def step(
            self,
            rng: Rng,
            problem: InstanceBase,
            population: SolutionSetBase,
            archive: ArchiveBase,
        ) -> int:
            pass

# </submodule classic>

# <submodule voronoi>
class voronoi:  # Proxy class that introduces typings for the *submodule* voronoi
    pass  # (This corresponds to a C++ namespace. All methods are static!)

    class VoronoiImageReconstruction(InstanceBase):
        def __init__(
            self,
            target_image: np.ndarray,
            width: int,
            height: int,
            min_num_cells: int = 10,
            max_num_cells: int = 100,
            init: Optional[AnyInit] = None,
            complexity_objective: bool = False,
            track_complexity: bool = False,
            kdtree_threshold: int = 50,
        ) -> None:
            pass

        def discrete_domain_sizes(self) -> np.ndarray:
            pass

        def continuous_lower_bounds(self) -> np.ndarray:
            pass

        def continuous_upper_bounds(self) -> np.ndarray:
            pass

        def continuous_init_lower_bounds(self) -> np.ndarray:
            pass

        def continuous_init_upper_bounds(self) -> np.ndarray:
            pass

        def image_data(
            self, solution: SolutionBase, scale: float = 1.0
        ) -> Tuple[np.ndarray, int, int]:
            pass

        def evaluate(
            self, rng: Rng, solutions: SolutionSetBase, indices: List[int]
        ) -> None:
            pass

        def add_random(self, rng: Rng, solutions: SolutionSetBase, count: int) -> None:
            pass

        def fitness(self) -> FitnessBase:
            pass

        def archive_fitness(self) -> ArchiveFitnessBase:
            pass

        def log_solution(self, os: io.IOBase, solution: SolutionBase) -> None:
            pass

    class YourCustomCrossover(classic.DiscreteCrossoverBase):
        """It is important to use the fully qualified name for the base class (i.e. goblin::classic::DiscreteCrossoverBase) so
        that the bindings are generated correctly
        """

        def __init__(self) -> None:
            """Autogenerated default constructor"""
            pass

    class YourCustomMutation(classic.DiscreteMutationBase):
        def __init__(self) -> None:
            """Autogenerated default constructor"""
            pass

# </submodule voronoi>
####################    </generated_from:amalgamation.h>    ####################

# </litgen_stub>
# !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!  AUTOGENERATED CODE END !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
